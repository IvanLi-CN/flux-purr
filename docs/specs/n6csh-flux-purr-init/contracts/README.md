# Contracts Index

This directory keeps interface contracts bound to spec `n6csh-flux-purr-init`.

- [http-apis.md](./http-apis.md): HTTP + WebSocket contract between firmware control plane and React console.

Notes:

- Contract files in this folder are source-of-truth for this spec.
- Cross-project docs may mirror these contracts, but behavior changes must be reflected here first.
