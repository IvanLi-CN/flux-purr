# Flux Purr 真实控制平面运行时（#m8r4q）

> 当前有效规范以本文为准；实现覆盖与当前状态见 `./IMPLEMENTATION.md`，关键演进原因见 `./HISTORY.md`。

## 背景 / 问题陈述

- PR #27 已把 Web、native USB daemon、USB CDC、WiFi provisioning、firmware flashing 与 monitoring 的长期架构沉淀到 `docs/solutions/device-control/web-native-wifi-bridge-console.md`。
- `#hhwq8` 只冻结 mock-first Web demo，不代表真实 transport、daemon、USB CDC、WiFi HTTP 或真实 flashing 已交付。
- 本规范冻结 Flux Purr 真实控制面 v1：同一领域契约通过 mock、browser Web Serial、USB serial、native `devd` 和 WiFi/LAN HTTP 暴露；LAN 仅适用于可信私网，Web 只根据能力启用操作。

## 目标 / 非目标

### Goals

- 定义 Web、firmware 与 `devd` 共享的 identity、network、status、USB JSONL、devd HTTP、firmware artifact 与 error envelope。
- firmware 提供 `web_serial` 与 `net_http` contract adapter，复用同一控制邮箱、热控 runtime 和 EEPROM WiFi 字段。
- `devd` 作为 localhost HTTP daemon，提供 USB/serial discovery、lease、monitor、WiFi bridge、artifact verify、dry-run 与 real flash command boundary。
- `flux-purr` 作为 released CLI，通过 `devd` 执行命令行硬件控制、用户级硬件记忆、USB 端口配置、artifact dry-run 与 guarded real flash。
- ADC calibration control family 由 `#jt8r2` 约束，并复用本规格的 lease、USB JSONL、devd HTTP、CLI 与 Web capability boundary。
- calibration live control 和 auto job 是本规格的一等 runtime contract；它们必须在 firmware USB JSONL、native `devd` HTTP、CLI、Web Serial 与 Web app 间保持 transport parity。
- Release 使用单一产品 tag `vX.Y.Z`，Web、firmware 与 host-tools 资产挂同一 Release，并通过 release manifest 的组件指纹避免无效升级。
- Web demo 保持轻量 bench console 形态，但通过 client/transport 层接入 mock、browser Web Serial 与 devd contract，并对危险操作做 capability gate。
- 无真机时必须能用 host tests、mock serial 和 devd dry-run 验证主要契约。

### Non-goals

- 不把 `#hhwq8` 扩张成 fleet 管理后台。
- 不在无硬件环境声明 USB/WiFi/flash 已完成真机验证。
- 不绕过 lease、confirmation、dry-run、artifact verification 或 secret redaction。
- 不把 WiFi PSK、host path 细节或 raw secret trace 写入 UI history、logs 或 errors。
- 不提供 LAN firmware flash、LAN 初始 WiFi provisioning、WAN 控制或设备端 TLS。

## 范围（Scope）

### In scope

- `firmware/src/control_plane.rs` 及 feature flags。
- `tools/flux-purr-devd/**` native daemon。
- `tools/flux-purr-devd/src/bin/flux-purr.rs` user CLI。
- `web/src/features/control-plane-demo/contracts.ts`、browser Web Serial client 与 transport client。
- `docs/interfaces/http-api.md` 当前 HTTP/devd/USB contract。

### Out of scope

- host power actions。
- 用户认证、多租户 fleet 管理和远端云服务。

## 需求（Requirements）

### MUST

- Runtime status MUST expose the shared thermal plant state (`missing|active|invalid`), active
  transaction identity, projection validity, and heater lock reason. Automatic model calibration
  MUST store a complete physically valid two-anchor transaction directly as `active`; no
  candidate, promotion, or user acceptance operation exists in the control plane.
- The protected model-calibration job is the only control-plane path allowed to heat while no
  active model exists. It requires a PPS APDO covering `20V` at `>=3A`; cancellation, disconnect,
  unsupported source, sensor fault, or malformed observation MUST immediately remove that
  authorization and stop heating.

- 所有 transport 暴露同一领域模型：`Identity`、`NetworkSummary`、`Status`、`FirmwareArtifact`、`ApiError`。
- USB serial frame 使用 newline-delimited JSON；需要响应的 request 必须带 `request_id`。
- `hello` 必须返回 protocol version、framing、identity 和 capabilities。
- WiFi config frame 和 devd WiFi endpoint 必须 redaction password/PSK。
- `wifi_state_v2` capability 表示设备支持版本化 WiFi 事实。`NetworkSummary` 必须包含 `configurationGeneration`、单调 `transitionSequence` 与有限安全枚举 `failureCode`；未知或畸形 snapshot 必须在 adapter 边界拒绝，不能由 `devd` 或 Web 猜测补齐。
- WiFi 状态由硬件无关的 `no_std` 状态机唯一发布。已配置设备对外只发布 `connecting|connected|error`；未配置设备发布 `disabled`。保存动作本身不是设备网络状态，Web 的提交 loading 只能表示等待设备事实，不能发布额外的“临时失败”状态。
- EEPROM/flash 中的 WiFi 配置在冷启动时必须作为初始配置装载，不得产生运行时重新配置 signal。设备取得首个 DHCP lease 后不得因残留启动 signal 主动断开；断线监听必须保留在 DHCP 完成与监听注册之间已经发生的 driver event，并据此进入自动重连，禁止继续发布与实际链路不符的 `connected`。
- 设备真实上电或显式复位后，在 EEPROM/flash 恢复和 WiFi 初始化完成前，USB `get_network` / `get_status` 必须返回可重试的 `startup_busy`，不得把默认配置伪装成 `disabled` 或空 SSID。打开、关闭或重新连接 USB Serial/JTAG 本身不得触发该启动流程；devd 必须等待真实版本化快照后才更新客户端状态。
- WiFi 连接事务计时固定为：断开旧连接最多 3 秒；每次关联最多 8 秒；每次 IPv4/DHCP 获取最多 15 秒；整个配置事务最多 3 次尝试、总计 30 秒，整体 30 秒截止时间优先于阶段计时。可恢复单次失败保持 `connecting`，只有三次尝试或 30 秒事务耗尽后发布一次 `error`。失败是终态，设备不会在同一 configuration generation 内自动恢复；必须收到新的配置才会重新进入 `connecting`。
- runtime config frame 和 devd runtime endpoint 必须能更新目标温度、当前 preset slot、`presets_c[10]`、主动散热开关、heater hold 状态、调试用手动 PPS 覆盖、RAM thermal control profile preview、EEPROM-backed saved thermal control profile，以及热失控告警确认 `faultAttentionAcknowledged`。
- 所有 transport 的 `Status` 必须回显 `faultAttentionPending`。该字段只表示温度已从 `>=420°C` 热失控回落到 `<420°C`、但告警尚未确认；`SensorShort / SensorOpen / AdcReadFailed` 不得置位。`faultAttentionAcknowledged=true` 只确认热失控告警与解除对应 attention/强制风扇锁定，不得在温度仍为 `>=420°C` 时解除绝对停热、fault-latch 或每 `1s` 的热失控提示。
- PT1000 温度换算必须遵循硬件 `3V3 -> 2.49 kOhm -> PT1000` 分压拓扑，以 `3300mV` 作为电路模型电源；板级 ADC 增益和偏移只能通过持久化 RTD ADC calibration 修正，不得通过修改分压电源常量吸收。
- calibration frame 和 devd calibration endpoint 必须能读取 channel-centered calibration state、编辑共享样本、编辑 A/B 槽位、切换当前激活槽位，并导入完整 calibration state。
- Web app 必须在目标下拉的底部只提供一个 `Add device` 入口；选择该入口后进入单独的 Add device 页面，并在该页面提供 WiFi、Web Serial 与 Bridge 三种新增类型。
- Web app 在 live 模式没有选中真实目标时，主工作区必须显示全宽设备选择页；该页上半部分显示 known devices 网格，空设备提示不得呈现为卡片，中间显示分隔线，下半部分以单行三卡片显示 WiFi、Web Serial 与 Bridge 三种新增类型，且不显示右侧全局日志列或额外的分区标题。
- Web app 必须通过 Add device 页面里的 Web Serial 类型提供显式 browser Web Serial 连接动作；连接成功后必须通过 USB JSONL 读取 identity、network、status，并用 `runtime_config` 直接控制目标温度、Settings preset slot / preset 温度 / preset 启用状态、主动散热开关与 heater hold 状态。
- Browser Web Serial 必须识别 firmware 明确定义的 `reset_reason=<reason>` 与 `panic=<reason>` 诊断行。已连接设备报告复位时，Web 必须清除复位前的目标状态并显示设备报告的原因；不得从普通串口文本、请求超时或 transport 断开自行推断复位原因。
- Web app 的 live Settings preset UI 必须以 firmware/devd/Web Serial status 回读为事实源；当 firmware 前面板在 preset 设置界面修改 slot、温度或禁用状态时，Web 必须通过 live status polling 更新回显；当 Web 修改同一数据时，firmware 必须应用到前面板 UI state 并触发重绘。
- Web Serial 连接成功后，如果当前选中的是无目标或 WiFi/Web Serial/Bridge pending target，Web app 必须切换到真实 Web Serial target，不得继续显示 pending Bridge/WiFi runtime。
- Browser Web Serial 直连不得声明 firmware artifact verify、dry-run 或 real flash 能力；这些操作仍必须走 `devd` capability gate。
- devd 默认只监听 `127.0.0.1`，mutating endpoint 必须携带有效 lease。
- devd 必须以 `serve` 子命令启动，默认 bind `127.0.0.1:30080`；flags 必须能配置 bind、显式 serial port、artifact root、dev CORS 和 real flash。
- devd 未显式传入 `--serial-port` 时必须保持 `AppConfig.serial_port=None`，不得从环境变量、用户配置、模板、示例或硬编码默认值隐式选择设备；`None` 表示没有固定 USB 目标，而不是关闭发现。daemon 必须枚举全部符合项目 USB 身份规则的串口作为未验证候选，但不得自动打开、租用、探测或选择其中任何端口。候选在固件 identity probe 成功前只能显示 transport locator，`identity.deviceId` 与 `identity.hostname` 必须为空；只有 operator 明确选择后才允许进入连接与身份验证流程。
- devd native serial discovery 必须只暴露当前明确授权的 MCU 端口；授权端口缺失时不得自动选择其它 `/dev/cu.*` 或 `/dev/tty.*` 设备。
- `flux-purr` CLI 必须为 status/runtime/wifi/flash/monitor 操作自动创建、heartbeat 和释放 lease，支持 human 输出与 `--json` 输出，不要求用户手填 `leaseId`。
- `flux-purr pd pps set --volts <decimal> --amps <decimal> --device|--hardware` 与 `flux-purr pd pps clear --device|--hardware` 必须通过 lease 写 runtime contract；`--volts` 只接受 `0.1V` 步进、必须落在硬件 `5V~28V` 边界内且不高于实时 source capability，`--amps` 只接受 `0.05A` 步进且不高于 source capability。
- `flux-purr hardware` 必须把 USB 设备记忆写入 OS 用户配置目录，`FLUX_PURR_HOME` 可覆盖；LAN record 使用独立字段持久化 base URL、hostname、last IPv4 和 redacted token。
- 默认 `required` pairing policy 下，WiFi Info 进入时必须生成并显示新四位码；离开该页立即使 code 失效。每个窗口最多五次失败，成功返回 EEPROM 稳定 token；只有 USB/devd token-reset 可清除 token 和全部 LAN lease。HTTP v1 必须预留 `optional`（无 code claim）和 `unavailable`（无 claim、匿名基础只读）策略，前端和 CLI 不得把当前 default 当作唯一可能。
- LAN public surface 仅限 health、pairing metadata 和 pairing claim；所有 identity/network/status/events 读取必须 bearer-auth。设备 HTTP 必须支持 2 个同时在途的客户端连接，使 Web 与 DEVD/CLI 可并发读取且不得因另一个读连接存在而被 reset。每个带 CORS/PNA 或 revision 的响应必须完整写入状态行、全部 header、空行和声明长度的 body；header 缓冲不足必须在构造时失败，绝不允许截断为 `200` 与空 body。所有控制写操作必须持有 30 秒设备 lease，并携带发送该写入前由串行 `GET /api/v1/status` 读取的 `X-Flux-Purr-Revision`；缺失 revision 返回 `428 revision_required`，revision 已过时返回 `409 stale_write` 且不得进入控制执行。控制邮箱 entry 必须携带已经通过 HTTP gate 校验的 lease ID，主控制循环在执行前再次验证同一 lease 仍 active；过期 entry 返回 `409 lease_expired`，不得触碰 heater、PD、calibration 或 EEPROM。LAN transport 只能把命令送入该单消费者控制邮箱，由主控制循环串行执行写入；不得直接修改 heater、PD 或 EEPROM runtime state。成功写入后 revision 单调递增，并通过响应头回传。
- WiFi station 的 driver buffers 必须按控制面流量有界配置；WiFi driver 或 LAN task 启动失败必须通过 `NetworkSummary.state=error` 暴露，并且不得阻断 USB JSONL control/recovery loop。
- Web direct-LAN 仅允许 Chromium HTTPS origin `https://flux-purr.ivanli.cc` 通过 CORS/PNA 请求 HTTP 私网；Safari 必须明确显示不支持。Token 不得出现在 URL、trace、export 或错误内。
- `demo=false` 的 Add device 页面必须提供可达的 `WiFi / LAN` 连接流，而不是 pending WiFi target 或文档说明：初始显示 WiFi、Web Serial 与 Bridge 连接方式，其中 WiFi 必须默认选中并以可感知的选中态呈现，因此设备 HTTP 根地址输入可见且有明确归属。选择 Web Serial 或 Bridge 时，不得把 WiFi 地址输入误归属给未选连接方式。Bridge 只代表选择本机 DEVD 传输方案，不能在点击时创建、选中或伪造设备；其二级面板必须先明确选择 `USB` 或 `WiFi / LAN` 路径，再从 DEVD 对应的已发现/已登记候选中人工选择具体设备，未选设备时连接动作必须禁用。切换路径必须清除上一条路径的设备选择，浏览器 direct LAN record 不得混入 DEVD bridge 候选。Web direct LAN 随后只读 `GET /health` 的匿名低频基础身份和 pairing policy，且此阶段不得要求、发送或展示四位码。设备返回 `required` 时，连接成功后才弹出配对码对话框；对话框必须显示已完成匿名连接的 IP 地址与设备 ID，并将后续 pairing 请求固定到该地址。`optional` 时以无 code claim 获取授权；`unavailable` 时保持基础只读且不展示配对码输入。claim 成功后 Web 必须以 bearer 读取 identity、network、status，将设备作为真实 LAN target 选中，并仅在成功取得 LAN lease 后启用写控制。直连 LAN target 不得暴露 WiFi provisioning、固件刷写或 USB token reset。
- Web LAN 设备发现包括本浏览器已配对并安全保存的记录恢复、operator 明确输入的地址，以及 operator 主动触发的受限私有 IPv4 CIDR scan。浏览器 scan 只能匿名并发请求候选地址的公开 `/health`，单次最多覆盖 256 个地址，必须可取消并使用固定并发数与单地址超时；不得携带 token、不得使用 mDNS、不得后台扫描，也不得启动或调用 `devd`。直连面板的地址与 CIDR 初始值不得伪造为某个设备或网段；operator 最后填写的设备地址和 CIDR 网段作为当前浏览器 origin 的非敏感本地表单偏好跨重载自动回填，但不会据此自动连接或扫描。清空任一输入必须删除对应偏好；这些偏好不得保存密码、四位配对码或 bearer token。CIDR 输入、扫描操作、进度和已验证结果必须始终可见，不得折叠。扫描结果行的主动作必须是“连接”，点击后直接复用设备地址连接表单的 `health -> pairing -> probe -> lease` 流程；连接开始后候选行可保持选中态并禁用重复提交，不得把候选直接写入顶部可控设备列表。只有匿名 health 成功后完成配对（如 policy 要求）、bearer probe 和 LAN lease 成功，才按稳定 `deviceId` 注册并自动选中目标。LAN 目标主名称使用固件 `identity.hostname`，依次回退到配对 session hostname 和 `deviceId`；IP 地址与传输方式只作为次要信息。配对、probe 或 lease 任一失败时，顶部目标与设备列表保持不变；同一 `deviceId` 更换 DHCP 地址时更新原记录而不产生重复项。CIDR 扫描必须使用独立且不嵌套的原生表单，回车与“开始扫描”按钮走同一动作；扫描进行中两种提交入口均禁用，取消仍为独立的非提交动作。`devd`/CLI 的 mDNS refresh 与显式 CIDR scan 是独立的 native discovery surface，不得与 Web direct LAN 面板混用。
- Web LAN 配对必须将 stable bearer token 仅保存在当前浏览器 origin 的本地记录中，按 base URL 隔离；operator 重新选择已记住地址时，浏览器必须先以匿名 `/health` 确认相同 `deviceId`，才可用该 token probe、恢复目标并取得 lease，不得再无条件索要配对码。地址确认成另一设备时必须删除旧 session，绝不把旧 bearer 发给新设备。任何 `401` 必须将对应凭据标记为失效并要求重新物理配对，但不得删除已记住的设备身份或其它设备记录。claim 后的 bearer probe 先并发读取 identity 与 network，再读取 status，并从响应头合并最新 `X-Flux-Purr-Revision`；浏览器不得超过设备的两个 HTTP worker。每次 direct-LAN 控制写必须在发送 PUT/POST 前串行读取当前 `GET /api/v1/status`，以这一响应的 revision 写入；不得复用 session cache 作为写 precondition。已配对 LAN target 从 Bridge 或其它连接方式切回时必须重新取得 lease；设备重启使内存 lease 失效时，已保存 token 的 direct LAN target 只有在 operator 显式重新选择该连接方式后才可重新请求 lease；心跳失败保持 expired 和只读，明确 conflict 也不得自动抢占。无关 status refresh 不得取消正在进行的 lease 请求。`stale_write` 时浏览器只可再读取一次当前快照、恢复界面事实并要求 operator 确认后重新提交；不得自动重放可能有副作用的写。设备地址连接和 required 配对码 claim 必须分别使用不嵌套的原生表单，回车与对应主按钮走同一校验、请求、loading 和错误处理路径；连接或 claim 进行中时两种提交入口均禁用，非法或不足四位配对码不得提交。密码、四位码和 bearer token 均不得进入 UI trace、错误、导出、URL 或视觉证据。
- 成功 probe 后，本地 LAN 记录必须同时保存最小身份摘要 `deviceId / hostname / firmwareVersion / buildId`。页面恢复时先以该摘要投影离线 WiFi/LAN 连接方式，再异步 probe 并原位更新；普通超时、离线、PNA、`401` 或设备重启不得让已知 LAN 通道从设备选择器消失。只有用户主动遗忘设备，或同一地址明确发现了不同稳定 `deviceId` 的目标设备时，才可删除或替换对应设备记忆。旧记录缺少身份摘要时保持兼容，并在下一次成功 probe 后补齐。
- Web 必须把 Chromium PNA/CORS 被浏览器阻断、设备地址不可达、pairing window 已关闭、四位码错误/锁定、设备响应格式异常、未经授权和 LAN lease busy/expired 表达为不同的、可操作的连接状态；transport/PNA 错误不得伪装为 WiFi station failure。LAN lease heartbeat 失败时，页面必须保留协议响应的安全错误说明；只有浏览器级无响应才可使用泛化 transport 文案。required pairing 的 claim 与后续 bearer probe 失败原因必须显示在当前配对对话框内，不得降级为对话框背后的通用连接失败。配对/探测/lease 在未完成期间必须有可关闭以外的 loading 状态，且同一时刻不得与终态结果重复显示。
- `devd`/CLI LAN discovery 必须保留 mDNS TXT metadata 与显式 scan 的来源；operator 可从发现结果或手动 URL 配对。固件 DNS-SD announcement 的 PTR 是 shared `IN` record，SRV/TXT/A 才设置 cache-flush；hostname、service instance、A 地址与 TXT device metadata 必须由 host-testable encoder 生成。Web 的 DEVD Bridge / WiFi-LAN 路径必须读取 daemon 的已登记 LAN registry，并始终提供“刷新服务”与 CIDR 网段表单；读取 registry 不得触发网络扫描，mDNS refresh 和 CIDR scan 只能由 operator 显式提交。发现进行中两种入口必须禁用以防重复请求，失败时保留既有候选并展示独立错误；候选主文案使用 hostname，地址和配对状态仅作为次要信息。发现候选只允许保留在桥接选择面板；未完成配对、设备探测与 LAN lease 前，不得加入顶部设备列表或伪装成可控 target。DEVD LAN 候选的 `POST /lan/devices/{id}/connect` 必须在 daemon 内使用已保存 token 依次完成 identity、network、status 验证，并只在三者均有效后返回已登记 bridge target；Web 收到该 verified record 后不得再创建一条用于识别的 DEVD lease，以免同一浏览器与自身的后续控制会话产生伪冲突。写控制的 lease 仅在 bridge target 已注册后按既有控制路径获取。该链路只能调用 DEVD LAN discovery/connect endpoints，不得混入浏览器 direct-LAN session 或 USB device records。native USB target 的顶部主名称同样必须使用固件 `identity.hostname`，缺失时回退到固件 `deviceId`；USB product descriptor 只属于发现层元数据，不得作为硬件名称显示。设备选择器必须按固件稳定 `identityId/deviceId` 合并同一物理设备；每个设备只显示一张卡片，卡片同时显示设备名称、设备 ID 和最多三种固定公开连接方式：`WiFi / LAN`（浏览器直连，默认优先）、`Web Serial`、`桥接`（由 DEVD 负责发现并在浏览器直连不可用时兜底）。DEVD 的 USB 与 WiFi/LAN 仅作为桥接按钮的内部来源，不得拆成两个公开入口。只有用户点击具体连接方式按钮才切换 target，不能把同一设备的不同 transport 渲染为重复设备。CLI 的 LAN 输出、registry、events 与错误均必须脱敏 bearer token；只有持有 native USB/devd lease 的 token reset 可以撤销所有 LAN session。
- DEVD bridge 对已配对 LAN record 的 runtime、calibration、calibration job 与 heater-curve 读写必须转发至设备 HTTP API，不能仅修改 daemon 的本地记录后报告成功。每次 bridge 写由 DEVD 创建短期设备 lease、让 LAN adapter 在远端串行读取当前 status revision、转发去除 daemon-local `leaseId` 的控制 payload，并在终态响应后释放该短期 lease。LAN WiFi provisioning 仍属于 USB/devd native target 的专属能力，bridge LAN 写入必须明确拒绝；设备返回的 HTTP 状态、`error.code`、`error.message` 与 `error.retryable` 必须透传，包括 `401`、`409 stale_write` 与 `409 lease_expired`，不得改写为本地 bridge error。
- 所有 direct-LAN 写入在 `stale_write` 后只可回读一次与被拒绝资源相同的设备事实：runtime 与 calibration job 回读 `status`，calibration 回读 `calibration`，heater-curve 与保存动作回读 `heater-curve`；原写入保持被拒绝，Web 不得自动重放任何可能有副作用的请求，operator 确认后才能重新提交。GitHub PR CI 与 `CI Main` 必须运行 firmware format/clippy/build、devd/CLI tests、Web check/unit/Storybook/build 与 E2E；CI 在 Storybook/E2E 前安装 Chromium。EdgeOne production deploy 只能消费同一次成功 `CI Main` 在 `main` 的 push 运行生成的 verified `web/dist` artifact，workflow_dispatch 只做验证不触发部署。
- Browser Web Serial 必须在稳定页面生命周期预读取既有浏览器授权端口；首次显式连接可复用唯一的既有端口，否则必须在触发点击的用户手势内同步请求浏览器原生选择器，不得在该调用前等待异步任务。Add device 页面始终表示添加另一连接：即使 Web Serial 已连接，卡片仍可用并必须强制打开原生选择器；只有连接事务进行中才可禁用防重复。选中新端口后先关闭旧会话再切换，新选择被取消时保留当前已连接设备。浏览器原生选择取消必须成为明确、可重试的“浏览器未确认串口设备”结果，不能显示底层原始异常；连接中与结果反馈不得被持久 heater lock 提示覆盖。Browser Web Serial 与 DEVD 打开 USB Serial/JTAG 时不得写入 DTR 或 RTS，连接只可开始有界只读 JSONL probe；连接实现不得把复位当作初始化步骤。连接完成后必须以 USB JSONL 读取 identity、network、status，并按稳定 device ID 与 WiFi/LAN、Bridge 合并为同一物理设备。
- Browser Web Serial 的连接成功必须结算同一通道此前残留的失败反馈：当前 target 已是 `connected` 时，“最近操作”不得继续显示 `Web Serial unavailable` 或先前 probe 超时；该结算不得覆盖 LAN、桥接或运行控制等无关操作的已完成反馈。
- 浏览器必须在 localStorage 记忆已成功 probe 的 Web Serial 设备身份，只允许保存稳定 `deviceId`、hostname、firmware version 与 build ID，不得保存串口对象、运行状态、配对 token 或其他凭据。稳定 `deviceId` 是唯一身份必填项；缺失的 hostname 回退到 ID，缺失的 firmware/build 元数据保存为 `unknown`，不得丢弃已确认设备。记忆记录在设备列表中投影为离线 Web Serial 通道；用户从设备卡片选择该通道时优先复用唯一已授权端口，但必须重新读取固件 identity 后才标记在线。实际 `deviceId` 相同才恢复原设备通道；不同时必须保留原离线记录，并把实际硬件注册、选中为另一台设备。Add device 的强制 chooser 语义不受快速重连影响。
- Web Serial 身份记忆必须在成功的 identity/network/status probe 同一连接事务内写入，不能依赖后续 UI effect；页面重新挂载、开发热更新或 target 列表重算后，已记忆的 Web Serial 连接方式仍必须与同一 device ID 的 LAN/Bridge 通道合并显示。DEVD bootstrap 或不可用占位不是可控制 target，不得在无可用 DEVD target 的页面写入“重新接管 devd 租约”或其它硬件控制失败反馈。若已进入 live DEVD 工作区后设备列表刷新失败，界面必须保留该占位作为只读诊断上下文并继续显示 Dashboard；所有写控制保持禁用，占位不得进入设备卡片、连接方式或已知设备列表。
- `flux-purr usb-port set` 必须写用户配置，并明确需要重启运行中的 `devd`。
- lease 必须有 heartbeat、TTL、过期 cleanup 和 conflict response。
- logs、trace、events 必须有固定上限；Runtime trace 只记录真实 bounded events 与 operator actions，`devd reachable`、当前连接状态或 scenario 重算摘要不得伪装成事件重复插入。所有事件在 adapter 边界统一为旧到新的时间顺序，并把时间格式化为本地 `HH:mm:ss`；Browser Web Serial 与 live operator action 在追加边界立即生成同一格式的本地时钟时间，只有 demo fixture 可使用固定时间。 “跟随尾部”启用时必须立即滚至最新事件，后续事件持续保持在可视底部，operator 主动向上滚动超过阈值后才自动关闭跟随。不得把 `live`、`web`、epoch timestamp、事件 ID 或内部序号显示在时间列。`devd` native USB JSONL TX/RX 必须作为 redacted `transport` events 进入 Runtime trace，保留 request ID、frame type 与 payload，WiFi password 等 secret 只能显示为 redacted。
- firmware artifact verify 必须校验 file existence、size 和 sha256，且只允许 artifact root 内的相对路径；real flash 必须先通过 dry-run。real flash 在应用写入前必须读取当前设备 partition table，并对当前 `flux_cfg` record 完成备份写入与逐字验证；应用写入后必须将同一 record 恢复至目标 `flux_cfg` 地址并再次逐字验证，即使地址未变化。任何预写、恢复或验证失败都必须明确失败，预写失败时不得开始 app 写入。
- devd artifact catalog 必须从本地构建产物计算 size/sha256，Web dry-check 必须调用 devd verify，而不是只做前端计时模拟。
- Web UI 必须在 capability 缺失、lease conflict、offline target、blocked artifact 时禁用危险操作并显示原因。
- Calibration mode control 必须只支持 PPS；Web 的 mode 入口固定为 `电压读数标定`、`温度标定`、`加热曲线标定`，owner-facing 术语不得回退到旧命名。
- Calibration mode live control 请求必须同时满足硬件 `5V~28V` 与实时 PPS capability。任何 transport 都不得实际发出不在该交集内的电压请求。
- `runtime_config` 必须支持 `calibration` 子对象，表达 mode、mode-owned PPS 请求、heater on/off、温度模式 ADC-hold 目标与稳定状态；旧 `manualPps*` 调试字段继续保留，但不承载新模式产品语义。`thermalProfileMode` 固定为 `auto|65w|100w`；`thermalControlProfile` 的 preview 继续是单一 RAM overlay，save/clear_saved 必须可携带 `bank=pps3a|pps5a` 写入指定 EEPROM bank。
- first-class calibration auto jobs 必须支持 `vin_adc_auto` 与 `heater_curve_auto` 两种 `kind`，并通过 `calibration_job` frame / `/calibration/job` endpoint 暴露 `idle|running|completed|failed|canceled` 状态、进度、样本数、下一目标电压与错误信息。
- Web app 必须用 URL 参数 `demo=true|false` 选择 demo 或 live 版本，并在 browser storage 记住最近一次显式 URL 选择；缺少 URL 参数时必须回填记住的版本参数，不得在没有显式 URL 参数切换的情况下自动改变版本。
- `demo=true` 必须只加载 demo scenario，不得启用 devd、Web Serial 或任何真实后端请求。
- `demo=false` 必须使用独立 live scenario，不得混入 demo fixture、degraded demo 数据或 daemon mock devices；真实后端返回的 mock devices 也不得显示为 live target。
- Product release workflow 必须产出单一 `vX.Y.Z` 或 `vX.Y.Z-rc.<sha7>` tag，不得继续创建新的 `web/v...` 或 `fw/v...` tag。
- Product release manifest 必须记录 Web、firmware、host-tools 组件的 `sha256`、`contentSha256`、`sourceSha`、`protocolVersions`、`changedSincePrevious` 与 `updateReason`。

### SHOULD

- devd scan 应在授权端口范围内利用 serial USB metadata 构造稳定 ID。
- 后续 direct firmware HTTP 若落地，返回 shape 应与 devd bridge endpoint 共用 Web parser。
- Web app 验证应覆盖 nominal、devd unavailable、lease conflict、monitor/trace 与 firmware blocked/warning states。固件具备 WiFi station 能力后，live Settings 在选中具备 `wifi_config` capability 的 native `devd` USB target 时显示 WiFi provisioning；若缺少 `wifi_state_v2`，表单与最后设备事实保留可见，但所有配置控件必须禁用并明确要求更新协议，不得提交或猜测状态。具备 `wifi_state_v2` 且 USB lease 有效时，它才可通过现有 USB bridge 提交 SSID 和密码；自动重连由固件固定启用，不在 Web、devd、CLI 或 USB JSONL 中作为配置项出现；不得在 direct LAN 或 Web Serial target 暴露。设备 set receipt 必须包含刚发布的版本化 `NetworkSummary`，Web 只接受同代或更新的 snapshot，绝不制造 WiFi 状态或本地判定 WiFi 超时。设备公开状态仅为 `disabled|connecting|connected|error`；配置事务遵循三次尝试、总计 30 秒契约，可恢复失败继续表示 `connecting`，事务耗尽只成为一次 `error`，失败后同一 generation 不得自动恢复。提交、等待和失败后当前页面保留密码；设备确认 `connected` 或清除后的 `disabled` 才清空内存草稿，重载时只允许根据设备返回的 UTF-8 密码字节长度显示等长掩码。密码内容不得进入 response、feedback、event、trace、export 或错误。RSSI 只在 WiFi state 为 `connected` 时显示；USB/serial transport 错误必须通过 connection/transport issue 表达，不得覆盖最后一次 WiFi network state。清除已保存 WiFi 必须复用同一 USB lease 并要求显式二次确认。Add device 页面中的 WiFi 与 Bridge 类型在未绑定真实 transport 前只能展示待绑定状态，不得伪装为已连接硬件；Bridge 还必须覆盖 USB/LAN 路径切换、具体目标选择、空候选和未选择禁用状态。

## 接口契约（Interfaces & Contracts）

### HTTP / devd

- `GET /health`：返回 daemon identity、version、uptime、event/log/trace limits。
- `GET /api/v1/devices`：扫描并返回 known devices。
- `POST /api/v1/devices/:id/bind`、`connect`、`disconnect`：管理 daemon-local device record。
- `POST /api/v1/devices/:id/leases`：创建 lease；`POST /api/v1/leases/:lease_id/heartbeat` 续租；`DELETE /api/v1/leases/:lease_id` 释放。
- `GET /api/v1/devices/:id/identity|network|status`：读取同一领域契约；status 包含 `faultAttentionPending`，native serial 的物理 USB read 必须携带 active `lease_id`。
- `GET|PUT /api/v1/devices/:id/calibration`：读取、编辑共享样本与 A/B 槽位的 ADC calibration state；细节见 `../jt8r2-adc-calibration-control-plane/SPEC.md`。
- `GET|POST /api/v1/devices/:id/calibration/job`：读取、启动或取消 calibration auto job；job 细节见 `../jt8r2-adc-calibration-control-plane/SPEC.md`。
- `GET /api/v1/devices/:id/events`：SSE 输出 bounded events。
- Firmware LAN: WiFi STA 默认 DHCP，并发送 MAC 派生 hostname；USB WiFi config 可选 static IPv4，`staticIpv4` 缺省保留当前地址模式、`null` 清除静态地址并回到 DHCP、对象设置静态地址。设备通过 `_http._tcp.local` 广播 `api=v1`、`path=/api/v1`、`pairing=frontpanel` 和 stable `device` TXT metadata。`GET /health` 是匿名低频摘要，只返回基础 identity 与 `{ pairing: { mode: required|optional|unavailable, active, attemptsRemaining } }`，不得返回 runtime telemetry、code 或 token；`GET /api/v1/pairing`、`POST /api/v1/pairing/claim` 也是公开路由。当前 default `required` 只在 WiFi Info 可见时接受四位 code；`optional` 接受空 claim；`unavailable` 拒绝 claim 并保留 health 读取。`GET /api/v1/identity|network|status|events` 是 bearer 路由；`POST|PUT|DELETE /api/v1/leases` 管理单 writer lease；runtime、calibration、heater curve 与 thermal profile 写入要求 bearer + `X-Flux-Purr-Lease` + `X-Flux-Purr-Revision`。单一 TCP acceptor 使用两个静态 TCP socket，支持两个同时在途连接；identity 与 network 从发布快照直接响应，不消耗唯一的 mutation-capable workspace。status、SSE 与写入共用一个静态 workspace，控制邮箱仍是唯一写入序列化点。该有界模型允许浏览器并发 identity/network probe 与 DEVD/CLI 低频读取；额外连接在 socket 归还前受 TCP 有界排队，不得把资源等待误报为 PNA 或 WiFi station 失败。浏览器 probe 默认并发读取，遇到旧固件的传输级单连接失败时只允许一次串行兼容重试；认证、协议与 lease 错误不得被重试掩盖。每个 endpoint 的允许 HTTP method 必须在 bearer、lease、revision 和控制邮箱 dispatch 之前验证，method mismatch 返回 `405`，不得借由错误 method 触发任何写入。HTTP task 只能通过控制邮箱请求主循环，不得直接触碰热控、PD 或 EEPROM。
- `PUT /api/v1/devices/:id/wifi`：通过 USB bridge 写 WiFi config；request/response 不回显 password。`password` 字段省略表示保留设备已保存密码，显式空字符串才表示将密码清空并使用开放网络；只有清除动作才删除 SSID。live Settings 只能在 active native `devd` USB lease 下调用该 endpoint；SSID、密码输入框与保存/清除动作按钮固定为同一 `36px` 高度，表单无脏改动时保存动作必须禁用。
- `PUT /api/v1/devices/:id/runtime`：通过 USB bridge 写运行时控制项；支持 `target_temp_c`、`selected_preset_slot`、`presets_c`、`active_cooling_enabled`、`heater_enabled`、`manual_pps_enabled`、`manual_pps_mv`、`manual_pps_ma`、`fault_attention_acknowledged`、`calibration` 与 `thermal_control_profile` 的部分更新。
- `GET /api/v1/artifacts`：返回 daemon 可见的本地固件构建产物 catalog，包含 file kind、path、size、sha256 与可选 flash address；本地 ESP32-S3 release ELF 必须作为 `elf` artifact 走 `espflash flash`。
- `POST /api/v1/artifacts/verify`：校验 catalog/artifact 文件。
- `POST /api/v1/devices/:id/flash`：`dry_run=true` 只校验；`dry_run=false` 必须先有同 artifact 的通过记录。

### CLI

- `flux-purr devices`：列出 `devd` 当前可见设备。
- `flux-purr identity --device <id>|--hardware <saved-id>`：通过 leased identity endpoint 读取设备身份。
- `flux-purr status --device <id>|--hardware <saved-id>`：通过 leased status endpoint 读取状态，包括热失控待确认的 `faultAttentionPending`。
- `flux-purr runtime get|set`：读取或部分更新目标温度、preset、主动散热与 heater hold，并支持显式发送 `faultAttentionAcknowledged=true` 确认热失控告警。
- `flux-purr pd pps set|clear`：设置或清除调试用手动 PPS 覆盖；设置路径要求 source status 已回报 PPS capability，且电压在硬件 `5V~28V` 与 capability 交集内，请求电流在 APDO current capability 内。
- `flux-purr thermal profile preview|clear-preview|save|clear-saved`：通过 leased runtime endpoint 设置或清除 RAM thermal control profile preview；save/clear-saved 的 `--profile-mode 65w|100w` 总是显式下发对应 bank。`--profile-mode auto` 必须先应用 auto mode 并从同一 leased runtime 请求回读 resolved bank，再向该 bank 执行 save/clear-saved；不得使用切换 mode 前的旧 status 推断持久化 bank。
- `flux-purr thermal self-test`：通过 `devd` 控制 Flux Purr，并通过 released `isolapurr` 工具的显式 LAN URL 路径准备 IsolaPurr bench source。单次 live run 工作目录只生成 thermal self-test 数据文件与候选 profile，不生成额外本地网页；owner-facing 冻结 baseline bundle 以浏览器可直接打开的 `index.html` 为 canonical report，并同时提交 `run.bundle.json`、`samples.ndjson` 与 `thermal-profile.accepted.json`。`--profile-mode auto|65w|100w` 保持低层 source 电压/电流覆盖，并允许用 `--source-power-watts` 显式指定 source capability ceiling；source capability 设置必须读回目标 watts、PD Fixed、PPS 与 auto-follow。source-side 重新上电必须使用 IsolaPurr runtime power gate：先 `isolapurr power runtime output --enabled false`，用 `runtime.output_enabled=false` 和 USB-C 零电流/零功率或非 `ok` 状态证明不再出力，等待 `2s`，再 `isolapurr power runtime output --enabled true`；不得把 `power output manual --usb-c-path disconnected` / `power output auto` 当作标准掉电再上电流程。报告和 samples 必须记录 selected mode、resolved bank、detected source class、source preset/readback 与 profile save provenance，同时还必须记录逐目标 `Approach` 参考曲线元数据：至少包括 `approachCurvePreferredMs`、`approachCurveLimitMs`、`approachCurveMaxAboveC`、`approachCurveMaxBelowC` 与 `approachCurveTailUsesHalfFloor`。`parameters` 必须显式回显 `holdSeconds`、`stageTimeoutSeconds`、`warmupTimeoutSeconds`、`runtimeRearmAttempts` 与 `cooldownTimeoutSeconds`，并维持“3 分钟 fixed round timeout + 独立 warmup timeout + 预算只裁 cooldown”的 timing contract。有界 `/status` 重试失败时必须把 `status_request_failed` 记录为 stop reason，而不是静默吞掉。对于 preliminary review bundle，`run.bundle.json` 顶层必须显式回显 `kind=thermal_self_test_preliminary_bundle`、`bundleDisposition=preliminary_review` 与 `acceptedProfileRole=review_candidate_snapshot`，且每个 target 必须允许附带 `holdCheck`，至少包含 `confirmRunId`、`passed`、`failureReason`、`holdSeconds`、`maxOvershootC`、`holdPeakToPeakC`、`firstHoldAtMs`、`holdMedianOutputPermille`、`holdP90OutputPermille`、`approachSource`、`holdSource`、`sourceRunPath` 与 `stopReason`。auto 只在 PPS APDO 覆盖 `20V` 且 advertised `ppsMaxMa >= 5000` 时解析 `pps5a`；显式 `65w` / `100w` 不回退或形成运行互锁。
- `flux-purr thermal tune`：执行 owner-facing 的 `100w / pps5a` 5A full-batch thermal tuning / preliminary review orchestration；`flagship-tune` 只保留为兼容别名。默认同等级调优目标固定为 `60 / 80 / 100 / 120 / 140 / 160 / 180 / 220 / 240°C`，物理目标集合写入 `tuningTargetsC`，默认实际执行顺序固定为递归二分的 `60, 240, 140, 100, 80, 120, 180, 160, 220` 并写入 `tuningExecutionOrderC`。调度规则固定为：先调当前区间两端点，再调中点；偶数区间取靠前中点；之后左半区优先、深度优先继续细分。每个 tuning target 从 cooldown wait 开始计入 `20min` per-target budget，循环执行 target-local scout、offline retune、`current + evidence-specific predicted point` batch compare。满足 warmup `100%`、动态 full-speed-to-stable gate 与 hard review metric gate 的 candidate 都必须进入 `60s` hold confirm；确认裕量只用于短测候选排序和优先级，`within_gate_low_margin` candidate 不得直接 accepted 或 passed，必须通过这一次完整 confirm 才能晋级。短 scout 或 batch candidate 只要 `maxOvershootC > 3.0°C`、`holdPeakToPeakC > 3.0°C`、stage 未 completed、settle time 缺失或超过动态门槛，就不得晋级。区间内子目标只有在该区间两侧边界目标都已 accepted 时才允许开始；未调过目标的 seed 只能来自最近两侧 accepted 目标点的线性插值。任一目标一旦 accepted，其最终 point 参数必须冻结，后续流程不得回写或共享到其它目标；若某目标 failed，则该目标所属子区间不得继续细分，但其它仍满足边界条件的独立区间仍可继续。若 scout 本身已经证明当前 profile 满足动态 gate 与 hard review metric gate，Rust orchestration 允许直接把当前 profile 提升到 `60s` hold confirm，而不是再消耗一轮冗余 batch retry。真实 HIL 默认不得设置独立调参轮次硬上限；`--max-tuning-rounds` 只可用于显式 debug/retest。Host 只能依据固件持续 `heaterControlMeasurementGuarded=true`、采样率、source/runtime 或明确硬故障判定温度测量无效，所有原始样本必须保留；不得按 host 推断的温度形态隐藏或排除数据。`ThermalControlProfile` 的调优控制面参数必须 point-local materialize：`warmupReenterCentiC`、`holdEntryCentiC`、`holdExitCentiC`、`holdOnCentiC`、`holdOffCentiC`、`overshootCutoffCentiC`、`holdKpPermillePerC`、`holdKiPermillePerCTick`、`holdBlendTicks`、`holdReheatPowerPermille`、`approachLeadTicks` 与 `holdLeadTicks` 只允许从 point 生效；`settings` 只保留 `tempFilterAlphaPermille`、`approachMaxTicks`、`approachMinPowerRatioPermille`、`autoAdjustableWorkingFloorMv` 与 `heaterCurrentReserveMa`。命令输出 canonical preliminary review bundle：`index.html + run.bundle.json + samples.ndjson + thermal-profile.accepted.json`，顶层必须回显 `kind=thermal_self_test_preliminary_bundle`、`bundleDisposition=preliminary_review`、`acceptedProfileRole=review_candidate_snapshot`、`selectedMode=100w`、`resolvedBank=pps5a`、`detectedSourceClass=pps5a`、`tuningWorkflow=five_amp_batch`、`tuningTargetsC`、`tuningExecutionOrderC`、`temperatureSemantics`、`candidateDispositions`、`candidateReadyTargetsC` 与 `reportRuns`。`candidateReady=true` 只允许表示该 target 存在可复盘 candidate 且主展示结果满足 hard review metric gate：`maxOvershootC <= 3.0°C`、`holdPeakToPeakC <= 3.0°C`、stage completed、full-speed-to-stable settle time 不超过该目标动态门槛；有 point 或有有效样本本身不得单独产生 candidate-ready receipt。Owner-facing 报告必须使用 `reportRuns` 按唯一目标温度渲染 summary card、target tab、温度响应、控制输出、source telemetry、轮次参数与评价；target wall-clock 总耗时以分钟显示，所选实测轮次的 `approach` phase duration 必须单独展示。full-speed-to-stable 只允许在详情或诊断区标注为稳定窗口 gate 指标，不得冒充逼近阶段用时。Python `scripts/thermal_tuning*` owner-facing 调优/报告入口已经废弃并从正式执行面移除；真实 HIL、正式调优、正式报告生成与 owner-facing 交付只允许走 Rust CLI。
- `flux-purr thermal tune` preliminary report completeness：`reportRuns` 只定义每个物理目标温度的默认主展示 entry，并且必须按物理目标温度升序排列；它不得作为丢弃 raw tuning entry 的理由。Owner-facing HTML 的标题目标列表、summary card 与 target tab 必须使用同一升序目标顺序。HTML embedded data 必须同时包含完整 `rawRuns`，并在每个 target tab 内提供 raw entry 切换，使同一目标下每个 raw entry 的 samples、rounds、温度响应、控制输出、source telemetry、参数与评价都可直接查看；`rawRuns` 保留实际执行/审计顺序，不强制按温度排序。
- `flux-purr thermal tune` 的 per-target budget 是从 cooldown wait 开始、覆盖 scout、每个 batch candidate 与 hold confirm 的单一单调 deadline。batch candidate 不得重置或穿透 deadline；到期必须由持有 lease 的 runner 先关 heater、释放 lease、恢复 source，再以 `budget_exhausted` 收口 target。
- `flux-purr thermal tune` preliminary report result taxonomy：owner-facing 结果只允许二元 `passed` / `failed`。每个 raw entry 与 `reportRuns` 主展示 entry 必须写入 `reviewOutcome` 与 `reviewPassed`；`candidateReady=true` 或 `candidateDisposition=acceptance_passed` 必须映射为 `reviewOutcome=passed`，其它情况必须映射为 `failed`。`candidate_ready`、`budget_exhausted_without_candidate`、`environment_blocked` 与 `not_executed_without_accepted_bounds` 等内部分类只允许保留在 JSON 审计字段中，不得作为第三种结果状态展示，也不得出现在 summary card 主结论区域。
- `flux-purr thermal retune`：从既有 thermal self-test `run.json` 与 `samples.ndjson` 执行离线 replay，写入 `run.replayed.json` 与 `thermal-profile.replayed.candidate.json`。当传入 `--apply-preview --device <id>|--hardware <saved-id>` 时，CLI 必须先写 replay 产物，再把 replayed candidate 与源 run 的 `selectedMode` 作为 RAM-only `thermalControlProfile.op=preview` 下发到目标设备。随后必须通过 `/status` 读回确认 profile mode/resolved bank、`thermalControlProfilePreview=true`、`profileActive=true`、`profileCoversTarget=true`、`profileSource=preview`，并逐字段核对当前目标的 effective point 与全局 settings。apply receipt 必须写回 `run.replayed.json.applyPreview`；preview、target 解析或任一 status readback 校验失败时命令必须失败，但 replay 产物必须保留，且不得执行 EEPROM save。
- 任何 thermal tune/self-test runtime arm 或 shutdown 写入都必须把 `PUT /runtime` 视为异步请求：同一 lease 下必须轮询 `/status` 直到目标温度与 `heaterEnabled` 状态一致；关热时还必须确认 `activeCoolingEnabled=true`。写入响应中的旧 status 不得直接作为 readback 结论。
- 一般 runtime 写入也必须以设备确认的 response snapshot 作为本次操作的事实结果：若固件因 thermal model 缺失或安全锁拒绝 `heater_enabled=true`，同一 response 必须返回 `heaterEnabled=false` 与锁定原因，客户端必须立即结算为拒绝，不得显示无限等待或先报成功再回滚。仅在 response 未能反映请求时，Web 才进入有界的 2.5 秒状态确认窗口；窗口到期必须显示一次明确失败。
- `flux-purr thermal report rerender-legacy`：从旧的 `preliminary-review-*` legacy bundle 或已存在的 `thermal_self_test_preliminary_bundle` 输入目录，重新写出 canonical owner-facing `index.html + run.bundle.json + samples.ndjson + thermal-profile.accepted.json`。该命令是 compliant preliminary review 报告的正式 host 入口；legacy 目录不得再直接充当最终交付页。
- `flux-purr calibration-mode ...`：提供 owner-facing 三模式入口；现有低层 `calibration ...` 与 `heater-curve ...` 原始命令继续保留。
- `flux-purr wifi set|clear`：通过 leased WiFi endpoint 写入或清除 WiFi 配置，输出必须 redaction password。
- `flux-purr flash`：默认 dry-run；真实烧录必须显式 `--no-dry-run --confirm FLASH` 且 daemon 启用 real flash。授权的 ESP32-S3 Native USB Serial/JTAG `cu.usbmodem*` 路径必须通过 `espflash --before usb-reset` 自动进入烧录流程，不得要求人工切换下载模式；其他串口保留 `default-reset`。需要保留设备持久化数据的正式烧录仅支持此 `flux-purr -> devd` 路径；直接 `mcu-agentd flash`、直接 `espflash` 或 erase-chip 不属于该保证。
- `flux-purr monitor`：读取 bounded event backlog，不拥有长期未释放 lease。
- `flux-purr hardware available|recent|list|save|forget|path`：管理用户级 USB 硬件记忆。
- `flux-purr usb-port show|set`：查看或保存默认 USB serial port。

### Release manifest

- Product release tag 是 `vX.Y.Z` 或 `vX.Y.Z-rc.<sha7>`。
- Release assets 包含 Web bundle、firmware bundle、host-tools bundle 和 `flux-purr-release-manifest-<tag>.json`。
- `changedSincePrevious=false` 的组件不得被推荐为必需升级项。

### USB JSONL

- `hello`：device 主动或 host 请求；返回 protocol、framing、identity、capabilities。
- `request`：`request_id` + `op`，支持 `get_identity`、`get_status`、`get_network`、`get_lan_pairing_code`、`open_lan_pairing_window`、`close_lan_pairing_window`、`set_log_level`。三项 LAN pairing request 仅通过 active USB/devd lease 使用，LAN HTTP 不得提供等价入口。`open_lan_pairing_window` 必须让前面板实际进入 WiFi Info 页面、生成并返回当前四位码；`close_lan_pairing_window` 必须离开该页并立即令 code 失效；`get_lan_pairing_code` 只在该页面仍打开时返回四位码。这样已授权 CLI/agent 能完整准备 Chromium pairing，同时显示器始终表达真实安全边界；devd 传输事件必须脱敏该 code。
- `wifi_config`：`request_id` + `op=set|clear` + credential fields；`staticIpv4` 缺省保留、`null` 回到 DHCP、对象设置静态地址；response 只包含 redacted summary。
- `wifi_config` response 的 `wifi` payload 是 `{ wifi: RedactedWifiConfig, network: NetworkSummary }` receipt。`network` 是固件在接受配置时刚发布的 snapshot，`devd` 只能原样验证、持久化和转发，不能合成 `saving`。
- `runtime_config`：`request_id` + runtime fields；支持 `thermalProfileMode`、`thermalControlProfile.bank` 与 `faultAttentionAcknowledged`。`thermalProfileMode` 只允许 `auto|65w|100w`，`thermalControlProfile.bank` 只允许 `pps3a|pps5a`，HTTP mock 与 native serial 必须在 dispatch 前使用相同校验。status 返回 `thermalProfileMode`、`thermalProfileResolvedBank` 与 `faultAttentionPending`；`auto` 的 resolved bank 只由 advertised PPS capability class 计算，不使用 live current。该 capability class 必须来自同一 selected PPS APDO：先要求覆盖 `20V`，再优先最大电流、最大电压和最小电压。CH224Q 安全限压和 reserve 仍使用 live current。持久化到非 active bank 不得改变 `thermalProfileResolvedBank`；`faultAttentionAcknowledged=true` 的超时回读只有在 `faultAttentionPending=false` 时才可判为成功。
- `calibration_config`：`request_id` + calibration fields；response 返回 calibration state。校准领域契约见 `#jt8r2`。
- `calibration_job`：`request_id` + `op=start|cancel` + optional `kind=vin_adc_auto|heater_curve_auto`；response 返回 calibration auto-job 状态。
- `response`：回显 `request_id`，返回 result 或 error。
- `status` / `log` / `error`：device-origin async frame。
- USB JSONL 单帧上限为 `8 KiB`（包含换行）。firmware、native `devd` 与 Browser Web Serial 必须使用同一上限；该容量必须容纳完整 9 点、point-local 的 thermal profile preview/save 请求，超限请求必须在 transport 边界明确拒绝。`save` 必须保留所提供的每一个 `1..=10` point，不得按 profile bank 投影、插值重建或静默丢弃温度点。

### Browser Web Serial

- Web app 使用 Add device 页面里的 Web Serial 类型调用浏览器 `navigator.serial.requestPort()`；未支持 Web Serial 的浏览器必须保持 mock/devd 路径可用并禁用 Web Serial 类型。
- Web Serial port 使用 `115200` baud 打开，按 USB JSONL 一行一帧写入 `request` / `runtime_config`，并只消费匹配 `requestId` 的 `response`。
- Web Serial 连接事务必须有界：从点击连接开始，最多等待 `60s` 完成浏览器端口选择、端口打开和首次 identity/network/status probe。客户端不得主动写 DTR/RTS，不得以固定延迟等待或接受设备复位作为连接步骤；打开、关闭或重新连接 USB Serial/JTAG 不得改变设备运行状态。只读 probe 可在总预算内重试，但只有设备明确发布 `reset_reason=` 或 `panic=` 时才显示复位诊断。超时发布明确的连接错误，按钮恢复可重试，迟到的端口必须关闭且不得在页面上偷偷变成已连接。连接开始时必须立即显示等待浏览器选择串口的反馈，不得继续显示旧的 devd/LAN transport 错误。Browser Web Serial 必须接受完整 `8 KiB` USB JSONL 单帧；不得以更小的浏览器缓冲上限丢弃合法 status 回包。
- 设备选择器中的连接方式按钮表示明确的 operator intent。用户选择 WiFi/LAN、Web Serial 或桥接后，当前 target 必须保持为该连接方式；其它已连接 transport 的 probe、轮询、恢复或迟到状态不得覆盖这次选择。自动采用 Web Serial 只允许发生在 Web Serial 连接事务自身，事务完成后必须释放该自动选择意图。
- 直连 target 在 Web app 内标记为 `transport=serial`、`baseUrl=webserial://selected`、`leaseState=active`；该 active 表示浏览器持有当前 port，不等价于 `devd` lease。
- Direct Web Serial 控制项只包括 runtime control、manual PPS debug override 与 status polling；status polling 必须回读 target、preset、cooling、heater、`faultAttentionPending`、manual PPS/capability/error 与 power/network summary，并允许通过 `faultAttentionAcknowledged=true` 确认热失控告警，供 Web 与前面板设置界面双向回显。firmware recovery、artifact catalog、dry-run、real flash、daemon-local bind/connect/disconnect 不属于该直连通道。
- Direct Web Serial 还必须支持 calibration live control、calibration auto-job read/write、ADC calibration draft/apply 和 heater curve preview/save，以保证 calibration workbench 的 transport parity。

## 验收标准（Acceptance Criteria）

- Given 无硬件环境，When 运行 host tests，Then USB frame parsing、request ID matching、redaction、runtime config、status adapter、lease expiry、bounded buffer、artifact verify 与 serial authorization guard 均通过。
- Given devd mock target，When 创建 lease 并 heartbeat，Then lease 未过期前 mutating endpoint 成功，过期后返回 conflict/expired error。
- Given artifact hash 不匹配，When 调用 verify 或 flash dry-run，Then 操作被阻断且 error 不泄露无关 host path。
- Given Web Update 页，When 运行 dry-check，Then 浏览器必须调用 devd artifact catalog/verify endpoint，并展示 daemon 返回的校验结果。
- Given Web app，When 打开真实控制面页面，Then nominal、devd unavailable、lease conflict、monitor trace、firmware blocked/warning 状态都可见，Runtime trace 支持按 info/success/warning/danger 等级过滤。
- Given Chromium 在 `https://flux-purr.ivanli.cc` 打开 `demo=false` 且设备处于同一可信私网，When operator 从唯一的 `Add device` 入口进入 `WiFi / LAN` 并输入 RFC1918 IPv4 或 MAC 派生 `.local` hostname，Then Web 必须先通过 `GET /health` 显示匿名低频基础身份，且连接前不出现、发送或持久化四位码。设备 policy 为 `required` 时，连接后才弹出 WiFi Info 页仍有效的四位码对话框；`optional` 时无 code claim，`unavailable` 时无 claim 且保持基础只读。成功 claim 后 Web 保存脱敏本地 session、读取 bearer-protected identity/network/status、自动选中真实 LAN target，并取得 30 秒 LAN lease 后才启用 runtime、calibration、heater curve 与 thermal profile 写控制。
- Given Web 已配对一个 LAN target，When 页面重载或返回设备选择页，Then Web 只恢复当前 origin 已保存的 LAN records，并逐个 probe 可达设备；不得自动扫描 mDNS/CIDR、不得请求未保存地址、不得把 token 放入请求 URL，恢复探测不得自动切换当前目标。Given operator 在 WiFi direct LAN 面板明确启动 CIDR scan，Then Web 只对受限私有 IPv4 范围发出匿名 `/health` 请求并展示始终可见的 CIDR 输入、可取消进度和已验证设备，整个流程不调用 `devd`。点击扫描结果的“连接”直接启动与设备地址按钮相同的 health、配对、probe 和 lease 流程；连接未完成前顶部目标保持不变，只有全部成功后设备 hostname 才进入顶部列表并被选中，IP 与 transport 作为次要信息。任一连接、配对或 lease 失败都不得修改当前目标或设备列表；同一稳定 `deviceId` 更换 IP 时必须更新原列表项。设备地址与 CIDR 只作为非敏感本地表单偏好跨重载恢复，清空对应输入会删除该偏好，恢复偏好不得触发连接或扫描，也不得包含密码、四位码或 token；首次没有偏好时不得预填伪造地址或网段。Given 任一 target 返回 `401`，Then 保留该设备及 LAN 通道、停用旧凭据并显示需要重新物理配对。
- Given Web direct LAN 面板，When operator 在设备地址、CIDR 或 required 配对码输入框按下回车，Then 必须分别触发与“连接设备”“开始扫描”“配对设备”按钮完全相同的校验和请求路径；每条路径只发出一次请求，进行中再次按回车或点击按钮不得重复提交，非法地址/网段和不足四位配对码不得发出请求。
- Given Chromium PNA/CORS 被拒、设备地址不可达、WiFi Info 页面离开、四位码错误/已锁定、设备成功响应无法解析、lease busy 或 lease expired，When Web 执行配对、probe 或写入，Then UI 显示与原因相符的连接/配对/lease 状态；claim 或 bearer probe 的原因显示在配对对话框内，背景不得同时出现通用连接失败；不得显示 WiFi station failure、不得保留可写控制、不得同时显示 loading 与终态结果。Safari 必须只显示不支持直连私网控制的状态，不得发送 LAN 请求。
- Given `devd` 或 CLI，When operator 明确执行 mDNS refresh 或 CIDR scan，Then 结果携带 DNS-SD TXT metadata/source，scan 只覆盖显式给定且受限的私网 CIDR；在没有 operator action 时不得发生扫描。When operator 使用发现结果或手动 URL 配对，Then registry 仅保存 redacted token，并在 status/runtime/control 写入时自动取得、heartbeat 和释放 LAN lease。
- Given Web 已选择 DEVD Bridge 的 WiFi/LAN 路径，When 面板打开，Then 只读取 daemon 已登记 LAN registry，不自动执行 mDNS 或 CIDR scan。When operator 点击“刷新服务”或在 CIDR 表单提交，Then Web 分别只调用对应 DEVD discovery endpoint、合并稳定 ID 候选并显示 hostname；请求进行中不得重复提交，错误不得清空此前结果，且 direct browser LAN 记录与 USB records 不得进入该候选集合。
- Given devd 未配置固定 `--serial-port` 且系统存在多个符合项目 USB 身份规则的串口，When Web 打开 Bridge 的 USB 路径，Then 必须列出全部 transport 候选而不是零个或任意一个；每个候选只显示 USB 产品元数据与串口 locator，并在行尾提供独立“连接”按钮，不得伪造设备 ID。When operator 尚未点击某行连接，Then daemon 不得打开或探测任何串口，Web 顶部目标保持不变。When operator 点击连接，Then Web 必须以模态框展示 identity-first 连接进度，进行中禁用全部候选连接按钮并防止重复提交。只有 API version、USB protocol、稳定 `deviceId` 与 `identity/network/status` capabilities 均满足控制平面契约时，才继续探测状态；不满足时模态框必须结算为“未知设备”，明确禁止连接且不得注册 target。成功状态必须先留在模态框供 operator 确认，确认后才以稳定 `deviceId` 与已有 LAN/Web Serial transport 合并为同一设备卡片。
- Given Web 存在同一固件 `identityId` 的多个 transport 记录，When operator 打开顶部设备选择器或 known devices 页面，Then 只能看到一个设备卡片；卡片主文案为固件 hostname、次要文案为设备 ID，并且最多列出三种固定公开连接方式：`WiFi / LAN`、`Web Serial`、`桥接`，其中 `WiFi / LAN` 默认优先。连接方式区域固定保留三列，单个方式只占卡片宽度的三分之一，不得因其它方式暂不可用而拉伸为整行。DEVD USB 与 DEVD WiFi/LAN 只能合并到同一个 `桥接` 按钮。daemon 为缺席授权端口或启动等待生成的占位记录只允许进入诊断与日志，不得生成设备卡片、连接按钮或当前 target 候选；串口路径与 daemon record ID 仅是 transport locator，固件身份未验证时 `identity.deviceId` 与 `identity.hostname` 必须为空，不得用 locator 冒充设备身份。When operator 点击某个真实连接方式，Then 只切换到该 transport 的 target；同一设备的其它连接方式仍留在同一张卡片内，不得生成重复设备项。顶部触发器只显示一个可随展开状态旋转的箭头，不得同时叠加原生或 CSS 选择器箭头。
- Given 支持 Web Serial 的浏览器，When operator 在目标下拉底部选择 `Add device`、进入 Add device 页面选择 Web Serial 并选择 ESP32-S3 端口，Then Web app 通过 USB JSONL 读取 identity/network/status，并把目标温度、fan policy 与 heater hold 写为 `runtime_config`。
- Given Web Settings 与硬件前面板都显示 preset 设置界面，When operator 在 Web 选择 preset slot、修改 preset 温度或切换 enabled，Then firmware 通过 `runtime_config` 更新 `MemoryConfig` 与 `FrontPanelUiState`，前面板界面及时重绘，Web 从返回 status 或下一轮 polling 看到相同 `selectedPresetSlot` / `presetsC`。
- Given Web Settings 与硬件前面板都显示 preset 设置界面，When operator 在硬件侧切换 slot、调整温度或禁用 preset，Then firmware status 反映新的 `selectedPresetSlot` / `presetsC`，Web live Settings 在下一轮 polling 内更新回显，不再显示前端硬编码 preset。
- Given WiFi 设置显示提交成功、失败或清除完成的结果 Toast，When 没有新的 WiFi 操作覆盖该结果，Then Toast 必须在 5 秒后自动收起；loading Toast 与清除 WiFi 的二次确认提示不得自动收起。
- Given `wifi_state_v2` native `devd` target 的 WiFi set receipt 为 `connecting`，When 旧 generation 或旧 sequence 的 `error` 到达，Then 标题和 Toast 保持当前 transaction 的 loading；只有同代或更新 snapshot 的 `connected|error`，或清除后的 `disabled`，才能原位结算。Web 不设置 WiFi 结果期限，也不得将 USB/devd transport 错误显示为 WiFi 失败。
- Given live Settings 选中具备 `wifi_config` 与 `wifi_state_v2` 的 native `devd` USB target 且 lease 有效，When operator 提交 SSID 和密码，Then Web 使用当前 lease 调用 `PUT /wifi` 并仅以响应 receipt 的版本化 `NetworkSummary` 与之后不旧于它的设备 snapshot 展示结果。自动重连是固件固定策略，不接受客户端配置。`connecting` 只显示 loading；设备直接返回 `connected|error` 时可直接结算，不能因漏过中间状态而永久等待，也不能将提交前的终态冒充为本次结果。设备端最多三次尝试、总计 30 秒；可恢复单次失败仍为 `connecting`，在成功前绝不展示失败 Toast。设备结算一次 `error` 后，只有更高 generation 的新配置才能改写该结果，不能自动恢复。标题严格来自最新设备事实，操作反馈使用不参与文档流、顶部水平居中的可关闭 Toast；等待时有 loading 动画和 busy 语义，终态原位替换 loading、5 秒后自动关闭，二者不得叠加。保存或清除事务未收到设备终态前，所有写操作按钮保持原生禁用；仅被触发的按钮显示对应 loading 图标和进行中文案，另一按钮保持普通文案，避免并发写入。Web 不设置 WiFi 结果期限，USB/devd transport 失败仅显示 transport 问题。提交、等待和失败后保留页面内密码；设备确认 `connected` 时用同一 `NetworkSummary.ssid` 回填 SSID 并显示硬件返回长度的等长掩码，清除后的 `disabled` 清空两项草稿。密码框点击自动全选该掩码，删除任意字符都将整段清空；未有 SSID 或密码变化时保存按钮保持禁用，密码掩码未被编辑时 Web 省略 `password` 字段以保留设备原密码，只有显式空字符串才清除密码。切换设备必须清除上一设备的草稿、密码与操作状态。When 页面重载，Then Web 从设备 `NetworkSummary.ssid` 回显 SSID，依据 `wifiPasswordLength` 以密码输入框的真实掩码值显示等长掩码，不使用 placeholder 冒充，也不得取得密码内容；When target 缺失 USB/devd 或 `wifi_config` capability，Then WiFi 表单不显示；When target 具备 `wifi_config` 但缺少 `wifi_state_v2`，Then 表单保留可见、所有配置操作禁用并显示协议更新原因；When lease 不可用，Then 表单保留可见但不可提交。Given operator 选择清除，Then 必须二次确认后才经同一 lease 发出 `op=clear`。
- Given live 模式没有选中真实目标，When 打开 Dashboard、Settings 或 Update，Then 主工作区仍显示全宽设备选择页，不显示 Dashboard/Settings/Update 内容，不显示右侧全局日志列；WiFi、Web Serial 与 Bridge 三种新增卡片保持同一行，点击任一新增卡片进入 Add device 页面并触发对应新增动作。
- Given Add device 页面选择 Bridge，When operator 尚未选择路径和具体 DEVD target，Then 页面保持 Add Device 且顶部目标不得切换为 pending `Native bridge`。桥接面板只显示自身的发现、候选选择、连接和错误状态；后台 target polling、旧 transport 错误与全局“最近操作”不得出现在该面板。When operator 在 USB 或 WiFi/LAN 路径选择一个具体候选并确认连接，Then 目标选择器才显示该真实 bridge target；LAN 候选确认必须在桥接面板内反馈，路径切换后旧候选立即失效。Given Bridge 选择流程仍未完成，When operator 改选 Web Serial 并连接成功，Then 目标选择器显示真实 Web Serial target，Dashboard 显示真实 runtime，而不是继续显示 `Native bridge / BRIDGE`。
- Given Web Serial 直连 target，When 打开 Update 页，Then artifact verify、dry-run 与 real flash 仍因缺少 `flash` capability 被禁用或要求切换到 `devd`。
- Given CLI 指向 `devd` mock target，When 执行 devices/status/runtime/wifi/flash dry-run/monitor，Then CLI 自动 lease、输出可读 human 文本或 `--json`，且 secret 被 redaction。
- Given CLI 或 Web live target 具备 PPS capability，When operator 设置 `10.4V / 2.50A` 手动 PPS 覆盖，Then runtime status 回显 `manualPpsEnabled=true`、`manualPpsMv=10400`、`manualPpsMa=2500`、capability 范围和更新后的 PD request/contract；When 清除覆盖，Then status 回到自动 PPS 控制。
- Given 温度已从热失控回落到 `<420°C` 且告警未确认，When 任一 transport 读取 status，Then `faultAttentionPending=true`；When CLI、Web、devd HTTP 或 Web Serial 发送 acknowledge，Then 所有 transport 在下一次 status 中回显 `false`。
- Given 温度仍为 `>=420°C`，When operator 发送 acknowledge，Then 绝对停热、热失控 fault-latch 与每 `1s` 提示保持；ack 只记录确认并解除 attention 对应的强制风扇锁定。
- Given `SensorShort / SensorOpen / AdcReadFailed`，When 任一 transport 读取 status，Then 必须报告测温 fault 并保留最后有效温度，同时 `faultAttentionPending=false`；发送 acknowledge 不得改变测温 fault 状态。
- Given CLI 对 thermal profile 执行 preview，When firmware 接受 runtime config，Then status 回显 `thermalControlProfilePreview=true`；When clear-preview，Then status 回显 `false`，且该 preview 不写入 EEPROM。
- Given CLI 对 thermal profile 执行 save，When firmware 接受 runtime config，Then profile 写入 EEPROM-backed active thermal profile 并立即参与控制；When clear-saved，Then saved profile 被清除且不会影响当前 RAM preview。
- Given CLI 执行 thermal self-test dry-run 或 mock devd 测试，When 未显式传入 `--targets-c` 生成报告，Then live run 工作目录中的 `run.json`、`samples.ndjson` 与 `thermal-profile.candidate.json` 均存在，默认目标阶梯为 `60 / 140 / 220°C`，且排除 `300°C`；冻结 baseline bundle 时则必须改写为 `index.html`、`run.bundle.json`、`samples.ndjson` 与 `thermal-profile.accepted.json`。
- Given CLI 执行 `thermal retune --run-dir <dir>`，When `<dir>` 包含 thermal self-test 的 `run.json` 与 `samples.ndjson`，Then CLI 只执行离线 replay，并写入 `run.replayed.json` 与 `thermal-profile.replayed.candidate.json`；When 同时传入 `--apply-preview --device <id>|--hardware <saved-id>`，Then CLI 在 replay 产物已落盘后按源 run 的 `selectedMode` 执行 RAM-only preview，并通过 `/status` 读回验证 mode/bank、preview active、当前 target coverage、preview source 与 effective parameters，再把 `applyPreview` receipt 写回 `run.replayed.json`。target 解析失败、preview 失败或任一回读校验失败时命令必须 non-zero 退出，但 replay 产物必须保留。
- Given CLI 执行 `thermal report rerender-legacy --legacy-bundle-dir <dir>`，When `<dir>` 是 legacy preliminary bundle 或已有 compliant preliminary bundle，Then CLI 必须在不同的输出目录下重写 canonical `index.html`、`run.bundle.json`、`samples.ndjson` 与 `thermal-profile.accepted.json`，并保持 `kind=thermal_self_test_preliminary_bundle`、`bundleDisposition=preliminary_review`、`acceptedProfileRole=review_candidate_snapshot` 与原有 target/round/sample 审计信息。
- Given host 以多个 single-target tuning / confirm 结果生成 preliminary 审查报告，When 生成 review bundle，Then 顶层 `kind=thermal_self_test_preliminary_bundle`、`bundleDisposition=preliminary_review`、`acceptedProfileRole=review_candidate_snapshot`，并回显 `selectedMode=100w`、`resolvedBank=pps5a`、`detectedSourceClass=pps5a`；每个 target 条目都必须带 `holdCheck`，且 `thermal-profile.accepted.json` 只表示当前候选快照，不得冒充 committed accepted baseline 或 EEPROM saved bank。
- Given CLI 执行真实 thermal self-test，When target ladder 在 preview 模式下全部通过验收，Then 单候选模式结束时的 `thermalControlProfile.op=save` response 必须回显 `thermalControlProfilePreview=false`，`run.json.profilePersistence` 必须为 `saved_tuned_candidate`；每个 stage 必须满足离开 full-speed warmup 后 `<=10s` 进入 hold、最大过冲 `<=3.0°C`、以及进入 hold 后连续 `60s` 峰峰值 `<=3.0°C`。同时，该 stage 的报告必须证明 Approach 参考曲线首选时长是 `5s`、硬上限是 `10s`，并给出该曲线相对实测温度的上下偏差。批测模式不得保存任一候选；任一 stage 或基础设施失败时不得声称 profile 已保存。
- Given Web 或 CLI 打开 calibration workbench / mode commands，When operator 进入 `电压读数标定`、`温度标定` 或 `加热曲线标定`，Then runtime status、USB JSONL、devd HTTP、CLI 与 Web Serial 都表达相同的 mode live state。
- Given calibration live control 请求超出硬件 `5V~28V` 或实时 PPS capability，When operator 通过 Web、CLI、devd HTTP 或 Web Serial 提交，Then Web 以受限控件和 inline error 阻止、CLI 报错退出、firmware/devd 拒绝请求，且不会发出非法电压请求。
- Given `电压读数标定` auto job 启动，When runtime capability 允许，Then job 以 `1V` 步进扫点并把样本写入 `vin_adc draft`。
- Given `加热曲线标定` auto job 启动，When稳定温区完成采样，Then runtime 先生成平滑后的 `heater_curve preview`，只有用户显式 `Save` 才写入 `active`。
- Given 用户保存默认 USB port，When 重启 `flux-purr-devd serve` 且未显式传入 serial port，Then daemon 只扫描该用户配置 port。
- Given product release 发布，When 查看 release assets，Then Web、firmware、host-tools 与 release manifest 同挂一个 `vX.Y.Z` Release；manifest 可区分 unchanged component。
- Given PR 收敛，When checks 完成，Then firmware、devd/CLI、release policy、Web build/test、Web app browser smoke 与授权端口硬件 smoke 均通过；WiFi provisioning 真机写入只通过 devd/USB smoke 覆盖临时 SSID set、clear、redacted event 和最终 disabled readback。
- Given HIL 验收，When 主人提供并授权确切 USB 端口，Then 通过 `flux-purr` CLI 经 `devd` 证明 identity/status、runtime write/readback/restore、artifact verify/dry-run、real flash、重启后 identity/status/events；未授权端口时不得创建 ready PR。
- Given LAN HIL 验收，When 主人提供已连接到可信 WiFi 的设备、同网 Chromium 与明确授权的 USB port，Then 验证 DHCP 地址或 MAC-derived hostname、mDNS TXT、WiFi Info 四位码、Chromium PNA pairing、无 token URL、重新加载后 token 恢复、authorized status/SSE、LAN lease conflict/expiry、runtime write/readback、离开 WiFi Info 后 code 立即失效，以及经 USB/devd token reset 后所有 LAN session 必须重新配对。未完成该闭环不得声称 WiFi-only LAN 控制已完成。

## 非功能性验收 / 质量门槛

- `cargo fmt --manifest-path firmware/Cargo.toml --all -- --check`
- `cargo clippy --manifest-path firmware/Cargo.toml --all-targets -- -D warnings`
- `cargo test --manifest-path firmware/Cargo.toml`
- `cargo test --manifest-path tools/flux-purr-devd/Cargo.toml`
- `bash .github/scripts/test-release-labels.sh`
- `bash .github/scripts/test-version-scripts.sh`
- `bun run --cwd web check`
- `bun run --cwd web typecheck`
- `bun run --cwd web build`
- Web app browser smoke against Vite preview with `devd` running.
- Calibration workbench browser smoke against both devd HTTP and direct Web Serial transports.
- Web LAN mock E2E 必须使用独立 HTTP v1 device fixture，不得启动、调用或依赖 `devd`；覆盖匿名 `/health` 连接、连接前无 code、`required|optional|unavailable` 策略、PNA/CORS blocked、四位码终态、token recovery/401 purge、lease busy/expiry、SSE reconnect、完整 LAN runtime control 和 Safari block。
- Storybook 必须为 LAN pairing 提供连接后 required code dialog、免配对码、pairing unavailable 的基础只读、PNA/transport failure、无效或锁定 pairing code、lease unavailable、恢复已保存设备与 mobile layout 的稳定故事和 play 覆盖；所有 token/code fixture 必须为测试值且不得进入视觉证据之外的日志。

## Visual Evidence

- 证据来源：Web app runtime 与 mock-only Storybook component。
- PR: none
- `assets/bridge-usb-identity-progress.png`：mock-only Storybook `App/ControlPlaneDemo/Live / Bridge USB connection progress`。每个 DEVD USB 候选行右侧提供独立“连接”按钮；触发后模态框锁定候选操作，并先读取固件身份、API 版本、USB 协议版本与必要 capability，未完成核验前不会注册或选择控制目标。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=1600x1000`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=bridge-usb-identity-verification-in-progress`，`evidence_note=per-candidate-connect-enters-identity-first-modal-before-target-registration`，`sensitive_exclusion=fixture-only-non-executable-device-location-no-live-hardware`，`story_id_or_title=App/ControlPlaneDemo/Live / Bridge USB connection progress`，`trim_result=viewport-capture-retained`，`submission_gate=owner-approved`。
- `assets/bridge-usb-unknown-device.png`：mock-only Storybook `App/ControlPlaneDemo/Live / Bridge unknown USB device`。身份响应缺失稳定设备 ID、协议版本不匹配或必要 capability 不完整时，模态框明确显示“未知设备”并禁止连接；顶部目标和已登记设备保持不变。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=1600x1000`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=bridge-usb-unknown-device-rejected`，`evidence_note=unverified-serial-device-cannot-be-promoted-to-a-control-target`，`sensitive_exclusion=fixture-only-non-executable-device-location-no-live-hardware`，`story_id_or_title=App/ControlPlaneDemo/Live / Bridge unknown USB device`，`trim_result=viewport-capture-retained`，`submission_gate=owner-approved`。
- `assets/bridge-target-chooser.jpg`：mock-only Storybook `App/ControlPlaneDemo/Live / Bridge target chooser`。点击桥接只展开 DEVD 二级选择，不创建或绑定 target；USB 与 WiFi/LAN 是独立路径，已发现的 USB 候选默认不选中，顶部仍显示 `No live target`，连接按钮保持禁用。指针选中只保留卡片自身选中态，不把键盘焦点描边遗留在卡片上；键盘激活则保留可见焦点。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=bridge-route-and-unselected-target`，`evidence_note=bridge-transport-selection-does-not-imply-device-selection-or-connection`，`sensitive_exclusion=fixture-only-device-path-no-live-hardware`，`story_id_or_title=App/ControlPlaneDemo/Live / Bridge target chooser`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=unchanged-ambiguous-border-page-margin-retained`，`submission_gate=owner-approved`。
- `assets/lan-browser-cidr-scan.png`：mock-only Storybook `App/LanPairingPanel/Browser Ip Scan Results`。WiFi direct LAN 面板由 operator 显式输入私有 IPv4 CIDR 并启动浏览器侧 `/health` 扫描，发现结果只回填 HTTP 根地址；故事和组件不启动、不调用也不展示 `devd`。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=element`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=explicit-cidr-scan-results`，`evidence_note=direct-browser-CIDR-discovery-is-separate-from-native-devd-discovery`，`sensitive_exclusion=fixture-only-private-address-no-token-or-code`，`story_id_or_title=App/LanPairingPanel/Browser Ip Scan Results`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=padded-capture-background-to-target-margin`，`submission_gate=owner-approved`。
- `assets/lan-target-scan-selection-page.png`：mock-only Storybook `App/ControlPlaneDemo/Live / LAN scan connection waits for pairing`。扫描结果行主动作显示“连接”，点击后直接进入 LAN 匿名连接与配对对话框；候选行保持选中态，但顶部仍保持 `No live target`，直到配对和 lease 完成。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=scan-candidate-connecting-before-lease`，`evidence_note=scan-result-connect-reuses-direct-lan-flow-without-registering-an-unleased-target`，`sensitive_exclusion=fixture-only-private-address-and-code`，`story_id_or_title=App/ControlPlaneDemo/Live / LAN scan connection waits for pairing`，`trim_result=unchanged-ambiguous-border`，`submission_gate=owner-approved`。
- `assets/lan-target-name-after-lease.png`：mock-only Storybook `App/ControlPlaneDemo/Live / LAN pairing registers device name`。health、配对、probe 与 lease 完成后，顶部主文案显示固件 hostname `flux-purr-001122334455`，次要信息显示 `WiFi · 192.168.1.42`，并自动选中 LAN target。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=lan-target-registered-after-lease`，`evidence_note=stable-hostname-is-primary-and-ip-transport-are-secondary-after-lease`，`sensitive_exclusion=fixture-only-private-address-no-token-or-code`，`story_id_or_title=App/ControlPlaneDemo/Live / LAN pairing registers device name`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=unchanged-ambiguous-border`，`submission_gate=owner-approved`。
- `assets/lan-browser-cidr-scan-persisted.png`：mock-only Storybook `App/LanPairingPanel/Browser Ip Scan Results`。直连浏览器 CIDR 扫描面板在扫描完成后仍保持 CIDR 输入、开始操作和已验证结果可见；截图仅使用固定私网夹具，表达的跨重载状态是 local form preference，而不是后台或恢复扫描。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=element`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=persisted-explicit-cidr-and-visible-results`，`evidence_note=browser-direct-health-scan-without-devd-or-mdns`，`sensitive_exclusion=fixture-only-private-address-no-token-or-code`，`story_id_or_title=App/LanPairingPanel/Browser Ip Scan Results`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=padded-capture-background-to-target-margin`，`submission_gate=owner-approved`。
- `assets/lan-browser-cidr-scan-spacing.png`：mock-only Storybook `App/LanPairingPanel/Browser Ip Scan` 的扫描结果连接态。结果行保留稳定的 sibling 间距、内边距和选中边框；主动作显示“连接”并直接启动设备连接，连接未完成前不注册顶部目标。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=element`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=scan-candidate-connect-action-with-spacing`，`evidence_note=scan-result-action-is-connect-and-result-row-spacing-remains-visible`，`sensitive_exclusion=fixture-only-private-address-and-code`，`story_id_or_title=App/LanPairingPanel/Browser Ip Scan`，`trim_result=padded-capture-background-to-target-margin`，`submission_gate=owner-approved`。
- `assets/lan-add-device-wifi-default.png`：mock-only Storybook `App/ControlPlaneDemo/Live / LAN connection choice`。Add device 初始状态呈现 `WiFi`、`Web Serial` 与桥接三种连接方式；`WiFi` 默认沿用工作台既有选中规则，以强调色文字和内凹按下态显示，LAN 地址输入与连接提交动作只归属于该已选方式。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=element`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=add-device-wifi-default-selected`，`evidence_note=visible-selection-explains-the-default-LAN-address-entry`，`sensitive_exclusion=no-device-data-or-credentials`，`story_id_or_title=App/ControlPlaneDemo/Live / LAN connection choice`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=unchanged-ambiguous-border-page-margin-retained`，`submission_gate=owner-approved`。
- `assets/wifi-state-v2-state-gallery.png`：mock-only Storybook `App/WifiNetworkSettings/State Gallery`，展示 `disabled|connecting|connected|error` 的完整设备公开状态集合；RSSI 仅在 `connected` 出现，已保存密码仅按 `wifiPasswordLength` 以掩码回显。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=none`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`sensitive_exclusion=password-not-rendered`，`story_id_or_title=App/WifiNetworkSettings/State Gallery`，`submission_gate=owner-approved`。
- `assets/wifi-state-v2-mobile-password-length.png`：mock-only Storybook `App/WifiNetworkSettings/Mobile Saved Password Length`，在 422px 宽的组件容器中验证输入与两个 36px 操作按钮完整换行且不越界；已保存密码只以 `wifiPasswordLength` 的掩码回显。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=component`，`requested_viewport=422px-component-container`，`viewport_strategy=component-container`，`margin_policy=require_margin`，`evidence_surface=component`，`sensitive_exclusion=password-not-rendered`，`story_id_or_title=App/WifiNetworkSettings/Mobile Saved Password Length`，`submission_gate=owner-approved`。
- `assets/web-app-devd-no-authorized-serial.png`：Vite Web App 连接当前租约 `devd`；授权端口缺失时只显示 daemon mock target，并在 trace 中标明没有授权 native serial target。
- `assets/web-app-devd-artifact-dry-check.png`：Vite Web App Update 页通过 `devd` 校验本地 ESP32-S3 固件产物，dry-check 返回通过。
- `assets/web-app-live-no-device-selection.png`：Vite Web App live `demo=false` 无真实目标状态显示全宽设备选择页；空设备提示以轻量文本呈现，单行三张新增卡片可见，右侧全局日志列和分区标题隐藏。
- `assets/web-app-live-preset-sync.png`：Storybook live Web Serial 场景覆盖 Settings preset 写入后从 status 回显；M5 被 Web 写为 disabled 后，summary、slot grid、selected editor 和 Runtime trace 保持一致。
- `assets/web-dashboard-manual-pps-request-current.png`：Vite Web App demo Dashboard 高级 PPS 面板显示两行 voltage/current request 控制、capability 动态范围、Apply/Clear 与请求电流说明。
- `assets/lan-pairing-desktop-success.png`：mock-only Chromium desktop 配对成功态，包含手动 HTTP 地址、四位码和稳定设备名；两个输入与提交按钮均为 36px 高。
- `assets/lan-pairing-mobile-success.png`：mock-only Chromium mobile 配对成功态，确认输入和提交控件在窄视口换行、均为 36px 高且无重叠。
- `assets/lan-pairing-lease-progress-desktop.png`：mock-only Storybook `App/LanPairingPanel/Chromium Pairing` 的桌面配对已完成、控制租约待确认状态；UI 不把领取 token 误报为已可控制设备。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=component`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`sensitive_exclusion=fixture-only-address-and-code`，`story_id_or_title=App/LanPairingPanel/Chromium Pairing`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=blocked-unsafe-to-crop-original-component-margin-retained`，`submission_gate=owner-approved`。
- `assets/lan-pairing-lease-progress-mobile.png`：mock-only Storybook `App/LanPairingPanel/Mobile Chromium Pairing` 的同一状态，故事把组件容器绑定为 `393px`，play 同时验证控件宽度不超过容器。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=component`，`requested_viewport=393px-component-container`，`viewport_strategy=component-container`，`margin_policy=require_margin`，`evidence_surface=component`，`sensitive_exclusion=fixture-only-address-and-code`，`story_id_or_title=App/LanPairingPanel/Mobile Chromium Pairing`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=blocked-unsafe-to-crop-original-component-margin-retained`，`submission_gate=owner-approved`。
- `assets/lan-pairing-required-prompt-desktop.png`：mock-only Storybook `App/LanPairingPanel/Required Pairing Prompt`。设备匿名 `/health` 已成功连接后才出现四位码对话框；对话框显示已连接 IP 地址和设备 ID，四位码未出现在连接前的表单中。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=element`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=required-pairing-after-anonymous-health`，`evidence_note=IP-address-and-device-ID-are-bound-to-the-connected-target`，`sensitive_exclusion=fixture-only-address-and-device-id-no-token-or-code`，`story_id_or_title=App/LanPairingPanel/Required Pairing Prompt`，`trim_result=already-within-target-margin`，`submission_gate=owner-approved`。
- `assets/lan-pairing-protocol-error.png`：mock-only Storybook `App/LanPairingPanel/Pairing Response Invalid`。设备匿名连接已完成，claim/probe 的成功响应无法解析时，具体协议错误显示在包含 IP 与设备 ID 的当前配对对话框内；背景不出现通用“无法连接”状态。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=element`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=pairing-success-response-invalid`，`evidence_note=claim-or-probe-protocol-failure-remains-actionable-inside-the-current-pairing-dialog`，`sensitive_exclusion=fixture-only-private-address-device-id-and-code-no-token`，`story_id_or_title=App/LanPairingPanel/Pairing Response Invalid`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=padded-capture-background-to-target-margin`，`submission_gate=owner-approved`。
- `assets/lan-form-enter-submit.png`：mock-only Storybook `App/LanPairingPanel/Enter Submits Device Address`，设备地址回车沿用连接按钮路径并展示连接后的 required 配对码对话框；画面同时保留 CIDR 表单，未调用 DEVD 或真实设备。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=component`，`requested_viewport=1024x400`，`viewport_strategy=explicit-component-capture`，`margin_policy=require_margin`，`evidence_surface=component`，`state=enter-submit-form-parity`，`evidence_note=address-enter-uses-the-same-connect-and-pairing-path-as-the-primary-button`，`sensitive_exclusion=fixture-only-private-address-and-device-id-no-token-or-code`，`story_id_or_title=App/LanPairingPanel/Enter Submits Device Address`，`trim_result=normalized-component-margin`，`submission_gate=owner-approved`。
- `assets/devd-lan-service-discovery.png`：mock-only Storybook `App/ControlPlaneDemo/Live / Bridge LAN service discovery`。DEVD Bridge 的 WiFi/LAN 路径显示已登记/发现的 hostname 候选、显式 mDNS 刷新和始终可见的 CIDR 表单；场景验证 registry load 不扫描网络，mDNS 与 CIDR 只由 operator 动作触发。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=devd-lan-explicit-discovery`，`evidence_note=bridge-discovery-is-explicit-and-separated-from-browser-direct-lan-and-usb-records`，`sensitive_exclusion=fixture-only-private-addresses-no-token-or-pairing-code`，`story_id_or_title=App/ControlPlaneDemo/Live / Bridge LAN service discovery`，`trim_result=unchanged-ambiguous-border-page-margin-retained`，`submission_gate=owner-approved`。
- `assets/native-hardware-name-toolbar.png`：mock-only Storybook `Components/ControlPlaneToolbar/Native Firmware Name`。顶部目标以固件 hostname `flux-purr-a0f262f20d6c` 为主名称，DEVD 与串口路径仅作为次要信息；触发器使用稳定双行布局，不复制下拉项 DOM，也不显示 USB product descriptor。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=element`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=native-firmware-name-selected`，`evidence_note=firmware-identity-is-primary-and-usb-descriptor-is-never-the-device-name`，`sensitive_exclusion=fixture-only-device-path-no-live-hardware`，`story_id_or_title=Components/ControlPlaneToolbar/Native Firmware Name`，`trim_result=padded-capture-background-to-target-margin`，`submission_gate=owner-approved`。
- `assets/device-picker-merged-connections.png`：mock-only Storybook `Components/ControlPlaneToolbar/Merged Device Connections`。同一固件 `device-1` 的 native DEVD、WiFi/LAN、Web Serial 与 bridge WiFi records 合并为一张设备卡片，三个公开连接方式各占固定一列；只有 Web Serial 的 `device-2` 仍只占第一列，不会拉伸为整行。顶部触发器只显示一个展开 Chevron。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=element`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=three-column-connection-layout-with-single-chevron`，`evidence_note=three-methods-fill-three-fixed-columns-while-one-method-keeps-one-third-width-and-the-trigger-renders-one-chevron`，`sensitive_exclusion=fixture-only-private-address-and-serial-path-no-credentials`，`story_id_or_title=Components/ControlPlaneToolbar/Merged Device Connections`，`bound_commit=d8b13c213e0f1bb402f8899690d3b44f4ecd7e98`，`trim_result=cropped_and_padded-require-margin`，`submission_gate=owner-approved`。
- `assets/lan-pairing-unavailable-mobile.png`：mock-only Storybook `App/LanPairingPanel/Mobile Pairing Unavailable`。`393px` 容器中设备连接后仅显示基础设备信息与低频只读提示，不渲染验证码输入或配对动作。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=component`，`requested_viewport=393px-component-container`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`sensitive_exclusion=fixture-only-address-no-token-or-code`，`story_id_or_title=App/LanPairingPanel/Mobile Pairing Unavailable`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`trim_result=cropped-and-padded`，`submission_gate=owner-approved`。
- `assets/wifi-network-settings.png`：Storybook 的 native-devd WiFi Settings 组件，SSID、密码、保存与二次确认清除均可见；两个输入和动作按钮均为 36px 高，自动重连不作为配置控件出现。
- `assets/web-serial-connecting-feedback.png`：mock-only Storybook `App/ControlPlaneDemo/Live / Web Serial connection timeout feedback` 的 Web Serial 等待态。点击 Web Serial 后立即显示等待浏览器选择串口的反馈，卡片进入 `connecting`，页面不继续展示旧的 devd transport 错误。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=none`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=web-serial-connecting-with-immediate-feedback`，`evidence_note=serial-selection-wait-is-explicit-and-does-not-inherit-stale-devd-feedback`，`sensitive_exclusion=no-live-serial-path-or-credentials`，`story_id_or_title=App/ControlPlaneDemo/Live / Web Serial connection timeout feedback`，`trim_result=trim_only`，`submission_gate=owner-approved`。
- `assets/web-serial-timeout-feedback.png`：mock-only Storybook `App/ControlPlaneDemo/Live / Web Serial connection timeout feedback` 的超时终态。60 秒有界连接事务报告具体超时原因，连接按钮恢复可重试，迟到的串口不会偷偷变成已连接。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=none`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=web-serial-timeout-retryable`，`evidence_note=bounded-serial-transaction-releases-retry-with-actionable-error`，`sensitive_exclusion=no-live-serial-path-or-credentials`，`story_id_or_title=App/ControlPlaneDemo/Live / Web Serial connection timeout feedback`，`trim_result=trim_only`，`submission_gate=owner-approved`。
- `assets/web-serial-port-selection-cancelled.jpg`：mock-only Storybook `App/ControlPlaneDemo/Live / Web Serial port selection cancelled`。首次浏览器串口选择被取消时，等待态结束、连接入口恢复可用，并显示明确的本地化原因；设备安全锁定反馈不会覆盖这条连接结果。已授权且唯一的端口会在下一次连接中自动复用，不重新触发选择器。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=none`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=web-serial-port-selection-cancelled`，`evidence_note=first-time-native-permission-cancellation-remains-actionable-and-preauthorized-single-port-reuses-without-a-second-chooser`，`sensitive_exclusion=no-live-serial-path-or-credentials`，`story_id_or_title=App/ControlPlaneDemo/Live / Web Serial port selection cancelled`，`trim_result=unchanged-ambiguous-border-page-margin-retained`，`submission_gate=owner-approved`。
- `assets/web-serial-known-device-reconnect.png`：mock-only Storybook `App/ControlPlaneDemo/Live / Known Web Serial reconnect`。设备卡片保留浏览器记忆的离线 Web Serial 通道；快速连接 probe 返回另一 stable device ID 时，实际硬件作为新卡片选中，旧设备继续保持离线，证明浏览器授权不会替代固件身份核验。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=1440x1000`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`state=remembered-web-serial-identity-mismatch`，`evidence_note=authorized-port-reuse-reprobes-firmware-identity-and-never-merges-a-replaced-device`，`sensitive_exclusion=fixture-only-device-identities-no-live-port-or-credentials`，`story_id_or_title=App/ControlPlaneDemo/Live / Known Web Serial reconnect`，`bound_commit=d8b13c213e0f1bb402f8899690d3b44f4ecd7e98`，`trim_result=unchanged-ambiguous-border-page-margin-retained`，`submission_gate=owner-approved`。
- `assets/wifi-password-length.png`：Storybook 的已连接 WiFi 状态使用硬件 `wifiPasswordLength` 在密码输入框内回显等长掩码，不额外展示字节数，也不返回或展示密码内容。
- `assets/wifi-password-mask-clean-save-disabled.png`：mock-only Storybook `App/WifiNetworkSettings/Saved Password Length` 展示已保存密码的真实掩码值、无 placeholder 属性以及未变更时禁用的保存按钮；点击密码会全选掩码，删除任意字符会整体清空并使表单变脏。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=component`，`requested_viewport=1024x240`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=clean-saved-password-mask-save-disabled`，`evidence_note=stored-password-mask-is-a-real-input-value-and-clean-form-cannot-submit`，`sensitive_exclusion=password-not-rendered`，`story_id_or_title=App/WifiNetworkSettings/Saved Password Length`，`trim_result=cropped-and-padded`，`submission_gate=owner-approved`。
- `assets/wifi-ssid-echo.png`：mock-only Storybook `App/WifiNetworkSettings/Saved Password Length`，设备已确认 `connected` 时，配置表单从 `NetworkSummary.ssid` 回填 `FluxPurr-Lab`；密码仅以保存的长度显示掩码。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=component`，`viewport_strategy=storybook-default`，`margin_policy=trim_only`，`evidence_surface=component`，`sensitive_exclusion=password-not-rendered`，`story_id_or_title=App/WifiNetworkSettings/Saved Password Length`，`bound_commit=8b5bfb3d2a4c75607a057cfb91055a1bd550fa07`，`submission_gate=owner-approved`。
- `assets/wifi-error-state.png`：Storybook 的 WiFi 错误态在输入带有旧 RSSI 时仍只显示“连接失败”，不渲染与状态矛盾的信号强度。
- `assets/wifi-terminal-recovery-contract.png`：mock-only Storybook `App/WifiNetworkSettings/Terminal Failure Stays Settled Until New Configuration` 的设备已结算错误态。固定 trace 在同一 `configurationGeneration` 内重复发布 `error` 时保持错误 Toast；只有更高 generation 的新配置重新发布 `connecting` 后才清除旧结果，随后只接受设备异步发布的 `connected`。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=desktop-storybook-default`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`state=terminal-error-requires-new-configuration`，`evidence_note=terminal-error-cannot-become-a-temporary-failure-without-a-new-configuration-generation`，`sensitive_exclusion=fixture-only-ssid-and-password-mask-no-real-credentials`，`story_id_or_title=App/WifiNetworkSettings/Terminal Failure Stays Settled Until New Configuration`，`trim_result=normalized_component_margin`，`submission_gate=owner-approved`。
- `assets/wifi-state-consistent.png`：Storybook 的快速提交成功态即使未渲染到中间状态，也会让设备标题与操作反馈同时收敛为“已连接”，密码输入保持不变。
- `assets/wifi-toast-no-layout-shift.png`：Storybook 的 WiFi 提交反馈使用固定定位 Toast；通知显示时不在组件下方增加反馈行，也不改变后续设置区域的位置。
- `assets/wifi-retry-success.png`：Storybook 的 WiFi 同一配置事务在可恢复单次失败后持续显示连接中；只有设备最终发布 `connected` 才原位替换为成功 Toast，事务结算为失败后不再自动恢复。
- `assets/wifi-stale-status-ignored.png`：mock-only Storybook 的 WiFi 设置在新提交已受理、旧 `error` 状态仍迟到时，标题保持“连接中”，loading Toast 保持等待；不会先显示本次失败。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=none`，`viewport_strategy=storybook-viewport`，`margin_policy=require_margin`，`evidence_surface=component`，`sensitive_exclusion=N/A`，`story_id_or_title=App/WifiNetworkSettings/Submitted Stale Error Awaiting Progress`，`submission_gate=owner-approved`。
- `assets/wifi-auto-reconnect-failure.png`：Storybook 的设备固定自动重连策略在设备耗尽三次尝试或 30 秒事务后，以设备发布的 error Toast 原位替换 loading；同一 configuration generation 不再自动恢复，页面没有自动重连开关。
- `assets/wifi-legacy-protocol-settings-page.png`：mock-only Storybook `App/ControlPlaneDemo/Settings / Legacy WiFi state protocol` 展示旧设备具备 `wifi_config` 但缺少 `wifi_state_v2` 时，WiFi 区域仍可见，SSID、密码、保存与清除均锁定，并显示协议更新原因；自动重连不是可配置控件。`source_type=storybook_canvas`，`target_program=mock-only`，`capture_scope=browser-viewport`，`requested_viewport=none`，`viewport_strategy=storybook-viewport`，`margin_policy=trim_only`，`evidence_surface=page`，`sensitive_exclusion=password-not-rendered`，`story_id_or_title=App/ControlPlaneDemo/Settings / Legacy WiFi state protocol`，`submission_gate=owner-approved`。

![LAN pairing desktop success](./assets/lan-pairing-desktop-success.png)
![DEVD bridge route and target chooser](./assets/bridge-target-chooser.jpg)
![Direct browser LAN CIDR scan results](./assets/lan-browser-cidr-scan.png)
![Direct browser LAN CIDR scan remains visible](./assets/lan-browser-cidr-scan-persisted.png)
![Direct browser LAN CIDR scan result spacing](./assets/lan-browser-cidr-scan-spacing.png)
![Web Serial waiting feedback](./assets/web-serial-connecting-feedback.png)
![Web Serial timeout feedback](./assets/web-serial-timeout-feedback.png)
![Web Serial port selection cancelled](./assets/web-serial-port-selection-cancelled.jpg)
![Known Web Serial reconnect verifies firmware identity](./assets/web-serial-known-device-reconnect.png)
![DEVD bridge LAN service discovery](./assets/devd-lan-service-discovery.png)
![Bridge USB identity verification in progress](./assets/bridge-usb-identity-progress.png)
![Unknown USB device rejected](./assets/bridge-usb-unknown-device.png)
![Native target uses the firmware hardware name](./assets/native-hardware-name-toolbar.png)
![Merged device card with connection methods](./assets/device-picker-merged-connections.png)
![LAN scan candidate connecting before lease](./assets/lan-target-scan-selection-page.png)
![LAN target hostname after lease](./assets/lan-target-name-after-lease.png)
![LAN add device with WiFi selected by default](./assets/lan-add-device-wifi-default.png)
![LAN pairing awaiting device lease on desktop](./assets/lan-pairing-lease-progress-desktop.png)
![LAN pairing awaiting device lease on mobile](./assets/lan-pairing-lease-progress-mobile.png)
![LAN pairing required code prompt after connection](./assets/lan-pairing-required-prompt-desktop.png)
![LAN pairing protocol error remains in the pairing dialog](./assets/lan-pairing-protocol-error.png)
![LAN native form Enter submission](./assets/lan-form-enter-submit.png)
![LAN pairing unavailable on mobile](./assets/lan-pairing-unavailable-mobile.png)
![WiFi state v2 gallery](./assets/wifi-state-v2-state-gallery.png)
![WiFi state v2 mobile password length](./assets/wifi-state-v2-mobile-password-length.png)
![WiFi retry success](./assets/wifi-retry-success.png)

![WiFi stale status ignored while awaiting connection](./assets/wifi-stale-status-ignored.png)

![WiFi automatic reconnect failure](./assets/wifi-auto-reconnect-failure.png)

![WiFi legacy state protocol remains visible and locked](./assets/wifi-legacy-protocol-settings-page.png)

![LAN pairing mobile success](./assets/lan-pairing-mobile-success.png)

![Web Dashboard manual PPS current request](./assets/web-dashboard-manual-pps-request-current.png)

![WiFi network settings](./assets/wifi-network-settings.png)

![WiFi password length](./assets/wifi-password-length.png)

![WiFi saved password mask with clean save disabled](./assets/wifi-password-mask-clean-save-disabled.png)

![WiFi confirmed SSID echo](./assets/wifi-ssid-echo.png)

![WiFi error state without stale RSSI](./assets/wifi-error-state.png)

![WiFi terminal failure remains settled until new configuration](./assets/wifi-terminal-recovery-contract.png)

![WiFi submission state consistency](./assets/wifi-state-consistent.png)

![WiFi toast without layout shift](./assets/wifi-toast-no-layout-shift.png)
- Chrome DevTools a11y snapshot on lease-managed `127.0.0.1:32082` against CORS-enabled `devd` `127.0.0.1:32083` verified the live Web page selects `USB JTAG/serial debug unit / DEVD` before daemon mock devices, reaches `LEASE ACTIVE`, displays real hardware PD/status values without mock simulation drift, shows WiFi state `DISABLED`, and includes bounded WiFi set/clear events in Runtime trace.

## 参考（References）

- `docs/solutions/device-control/web-native-wifi-bridge-console.md`
- `docs/specs/hhwq8-web-control-plane-demo/SPEC.md`
- `docs/interfaces/http-api.md`
