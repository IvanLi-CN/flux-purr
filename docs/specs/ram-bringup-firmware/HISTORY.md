# RAM Bring-up Firmware History

## Lifecycle

The topic is active. It establishes the physical CLI validation path that
replaces the removed physical RAM preview binaries while keeping persistent product
installation explicit and separately gated.
