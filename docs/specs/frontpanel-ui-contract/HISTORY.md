# Frontpanel UI contract history

## Legacy identity

- Former legacy ID: `223uj`.

## Lifecycle

- `active`: the visual contract remains the canonical 160x50 frontpanel baseline.

## Partial replacement

- Input and navigation behavior is locally superseded by `frontpanel-input-interaction`.
- Heater, fan, and dashboard runtime wording is locally superseded by `heater-pid-frontpanel-runtime`.
