[package]
name = "flux-purr-thermal-tuning-wasm"
version = "0.1.0"
edition = "2024"
description = "WebAssembly facade for Flux Purr thermal-tuning replay and verification"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
flux-purr-thermal-tuning-core = { path = "../thermal-tuning-core" }
wasm-bindgen = "0.2.106"

[dev-dependencies]
wasm-bindgen-test = "0.3"
