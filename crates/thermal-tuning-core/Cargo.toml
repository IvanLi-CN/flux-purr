[package]
name = "flux-purr-thermal-tuning-core"
version = "0.1.0"
edition = "2024"
description = "Deterministic no_std core for firmware-owned Flux Purr thermal tuning"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
sha2 = { version = "0.10.9", default-features = false }
